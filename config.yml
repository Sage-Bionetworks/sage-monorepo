default:
  database:
    host: localhost
    port: 5432
    user: postgres
    password: docker

test:
  database:
    name: iatlas_shiny_test

dev:
  database:
    name: iatlas_dev

shinyapps:
  database:
    name: ${DB_NAME}
    host: ${DB_HOST}
    port: ${DB_PORT}
    user: ${DB_USER}
    password: ${DB_PASSWORD}
