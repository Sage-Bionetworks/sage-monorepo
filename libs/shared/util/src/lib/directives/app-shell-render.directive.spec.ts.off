import { AppShellRenderDirective } from './app-shell-render.directive';

describe('AppShellRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new AppShellRenderDirective();
    expect(directive).toBeTruthy();
  });
});
