// Import from this file instead of the root entry point when the consumer cannot load Angular,
// such as a Playwright spec. The root entry point also exports the Angular-dependent SEO services.
export * from './lib/helpers';
