import { BoxplotDirective } from './boxplot.directive';

describe('BoxplotDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxplotDirective();
    expect(directive).toBeTruthy();
  });
});
