import { sharedTypescriptCharts } from './shared-typescript-charts';

describe('sharedTypescriptCharts', () => {
  it('should work', () => {
    expect(sharedTypescriptCharts()).toEqual('shared-typescript-charts');
  });
});
