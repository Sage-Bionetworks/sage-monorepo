type: object
description: An evaluation entity representing a user's assessment in a battle between two AI models.
properties:
  id:
    $ref: EvaluationId.yaml
  outcome:
    $ref: EvaluationOutcome.yaml
  createdAt:
    $ref: CreatedAt.yaml
  is_valid:
    $ref: IsValid.yaml
  validation_error:
    $ref: ValidationError.yaml
required:
  - id
  - outcome
  - is_valid
  - createdAt
