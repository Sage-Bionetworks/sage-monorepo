type: object
description: The information used to update a battle.
properties:
  title:
    $ref: BattleTitle.yaml
    nullable: true
  endedAt:
    $ref: EndedAt.yaml
    nullable: true
  effectiveValidationId:
    type: string
    format: uuid
    nullable: true
    description: Set the effective validation for this battle (admin only, nullable to clear)
