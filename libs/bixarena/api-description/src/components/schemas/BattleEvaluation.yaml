type: object
description: A battle evaluation describing the outcome of a matchup and any validation performed on it.
properties:
  id:
    $ref: BattleEvaluationId.yaml
  battleId:
    $ref: BattleId.yaml
  outcome:
    $ref: BattleEvaluationOutcome.yaml
  createdAt:
    $ref: CreatedAt.yaml
  isValid:
    $ref: IsValid.yaml
  validationError:
    $ref: ValidationError.yaml
    nullable: true
required:
  - id
  - battleId
  - outcome
  - isValid
  - createdAt
