type: object
description: A battle evaluation describing the outcome of a matchup.
properties:
  id:
    $ref: BattleEvaluationId.yaml
  battleId:
    $ref: BattleId.yaml
  outcome:
    $ref: BattleEvaluationOutcome.yaml
  createdAt:
    $ref: CreatedAt.yaml
  valid:
    $ref: Valid.yaml
required:
  - id
  - battleId
  - outcome
  - valid
  - createdAt
