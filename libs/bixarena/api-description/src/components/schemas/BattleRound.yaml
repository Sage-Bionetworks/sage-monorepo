type: object
description: A battle round containing the IDs of prompt and responses.
properties:
  id:
    $ref: BattleRoundId.yaml
  battleId:
    $ref: BattleId.yaml
  promptMessageId:
    $ref: MessageId.yaml
    nullable: true
  response1MessageId:
    $ref: MessageId.yaml
    nullable: true
  response2MessageId:
    $ref: MessageId.yaml
    nullable: true
  createdAt:
    $ref: CreatedAt.yaml
  updatedAt:
    $ref: UpdatedAt.yaml
required:
  - id
  - battleId
  - createdAt
  - updatedAt
