name: submissionTypes
description: Array of challenge submission types used to filter the results
in: query
required: false
schema:
  type: array
  items:
    $ref: ../../../components/parameters/ChallengeSubmissionType.yaml
