name: incentives
description: An array of challenge incentive types used to filter the results.
in: query
required: false
schema:
  type: array
  items:
    $ref: ../../../schemas/ChallengeIncentive.yaml
