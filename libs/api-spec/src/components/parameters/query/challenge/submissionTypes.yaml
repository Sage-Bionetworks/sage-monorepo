name: submissionTypes
description: An array of challenge submission types used to filter the results.
in: query
required: false
schema:
  type: array
  items:
    $ref: ../../../schemas/ChallengeSubmissionType.yaml
