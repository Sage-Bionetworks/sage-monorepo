# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test SchemaApi")

api_instance <- SchemaApi$new()

test_that("GetComponent", {
  # tests for GetComponent
  # base path: http://localhost/api/v1
  # Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String).
  # Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String).
  # @param component_label character The label of a component in a schema
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param include_index character Whether to include the indexes of the dataframe in the returned JSON string. (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetConnectedNodePairArray", {
  # tests for GetConnectedNodePairArray
  # base path: http://localhost/api/v1
  # Gets an array of connected node pairs
  # Gets a array of connected node pairs
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param relationship_type character Type of relationship in a schema, such as requiresDependency
  # @return [ConnectedNodePairArray]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetConnectedNodePairPage", {
  # tests for GetConnectedNodePairPage
  # base path: http://localhost/api/v1
  # Gets a page of connected node pairs
  # Gets a page of connected node pairs
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param relationship_type character Type of relationship in a schema, such as requiresDependency
  # @param page_number integer The page number to get for a paginated query (optional)
  # @param page_max_items integer The maximum number of items per page (up to 100,000) for paginated endpoints (optional)
  # @return [ConnectedNodePairPage]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetNodeDependencyArray", {
  # tests for GetNodeDependencyArray
  # base path: http://localhost/api/v1
  # Gets the immediate dependencies that are related to the given source node
  # Gets the immediate dependencies that are related to the given source node
  # @param node_label character The label of the source node in a schema to get the dependencies of
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param return_display_names character Whether or not to return the display names of the component, otherwise the label (optional)
  # @param return_ordered_by_schema character Whether or not to order the components by their order in the schema, otherwise random (optional)
  # @return [NodeArray]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetNodeDependencyPage", {
  # tests for GetNodeDependencyPage
  # base path: http://localhost/api/v1
  # Gets the immediate dependencies that are related to the given source node
  # Gets the immediate dependencies that are related to the given source node
  # @param node_label character The label of the source node in a schema to get the dependencies of
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param return_display_names character Whether or not to return the display names of the component, otherwise the label (optional)
  # @param return_ordered_by_schema character Whether or not to order the components by their order in the schema, otherwise random (optional)
  # @param page_number integer The page number to get for a paginated query (optional)
  # @param page_max_items integer The maximum number of items per page (up to 100,000) for paginated endpoints (optional)
  # @return [NodePage]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetNodeIsRequired", {
  # tests for GetNodeIsRequired
  # base path: http://localhost/api/v1
  # Gets whether or not the node is required in the schema
  # Gets whether or not the node is required in the schema
  # @param node_display character The display name of the node in a schema
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetNodeProperties", {
  # tests for GetNodeProperties
  # base path: http://localhost/api/v1
  # Gets properties associated with a given node
  # Gets properties associated with a given node
  # @param node_label character The label of the source node in a schema to get the dependencies of
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @return [NodePropertyArray]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetNodeValidationRules", {
  # tests for GetNodeValidationRules
  # base path: http://localhost/api/v1
  # Gets the validation rules, along with the arguments for each given rule associated with a given node
  # Gets the validation rules, along with the arguments for each given rule associated with a given node
  # @param node_display character The display name of the node in a schema
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @return [ValidationRuleArray]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetPropertyLabel", {
  # tests for GetPropertyLabel
  # base path: http://localhost/api/v1
  # Gets the property label of the node
  # Gets the property label of the node
  # @param node_display character The display name of the node in a schema
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param use_strict_camel_case character Whether or not to use the more strict way of converting to camel case (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetSchemaAttributes", {
  # tests for GetSchemaAttributes
  # base path: http://localhost/api/v1
  # Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String).
  # Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String).
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})
