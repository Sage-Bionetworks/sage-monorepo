# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test ManifestValidationApi")

api_instance <- ManifestValidationApi$new()

test_that("SubmitManifestCsv", {
  # tests for SubmitManifestCsv
  # base path: http://localhost/api/v1
  # Validates manifest in csv form, then submits it
  # Validates manifest in csv form, then submits it
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param component character A component in a schema, either the dsplay label or schema label
  # @param dataset_id character The ID of a dataset.
  # @param asset_view_id character ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
  # @param body data.frame .csv file
  # @param restrict_rules character If True, validation suite will only run with in-house validation rule. If False, the Great Expectations suite will be utilized and all rules will be available. (optional)
  # @param storage_method character file_and_entities will store the manifest as a csv and create Synapse files for each row in the manifest. table_and_file will store the manifest as a table and a csv on Synapse. file_only will store the manifest as a csv only on Synapse. table_file_and_entities will perform the options file_with_entites and table in combination. (optional)
  # @param hide_blanks character If true, annotations with blank values will be hidden from a dataset's annotation list in Synaspe. If false, annotations with blank values will be displayed. (optional)
  # @param table_manipulation_method character replace will remove the rows and columns from the existing table and store the new rows and columns, preserving the name and synID. upsert will add the new rows to the table and preserve the exisitng rows and columns in the existing table. (optional)
  # @param use_schema_label character If true, store attributes using the schema label If false, store attributes using the display label (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SubmitManifestJson", {
  # tests for SubmitManifestJson
  # base path: http://localhost/api/v1
  # Validates a manifest in json form, then submits it
  # Validates a manifest in json form, then submits it in csv form
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param component character A component in a schema, either the dsplay label or schema label
  # @param dataset_id character The ID of a dataset.
  # @param asset_view_id character ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
  # @param restrict_rules character If True, validation suite will only run with in-house validation rule. If False, the Great Expectations suite will be utilized and all rules will be available. (optional)
  # @param storage_method character file_and_entities will store the manifest as a csv and create Synapse files for each row in the manifest. table_and_file will store the manifest as a table and a csv on Synapse. file_only will store the manifest as a csv only on Synapse. table_file_and_entities will perform the options file_with_entites and table in combination. (optional)
  # @param hide_blanks character If true, annotations with blank values will be hidden from a dataset's annotation list in Synaspe. If false, annotations with blank values will be displayed. (optional)
  # @param table_manipulation_method character replace will remove the rows and columns from the existing table and store the new rows and columns, preserving the name and synID. upsert will add the new rows to the table and preserve the exisitng rows and columns in the existing table. (optional)
  # @param use_schema_label character If true, store attributes using the schema label If false, store attributes using the display label (optional)
  # @param body character A manifest in json form (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ValidateManifestCsv", {
  # tests for ValidateManifestCsv
  # base path: http://localhost/api/v1
  # Validates a manifest in csv form
  # Validates a manifest in csv form
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param component_label character The label of a component in a schema
  # @param body data.frame .csv file
  # @param restrict_rules character If True, validation suite will only run with in-house validation rule. If False, the Great Expectations suite will be utilized and all rules will be available. (optional)
  # @return [ManifestValidationResult]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ValidateManifestJson", {
  # tests for ValidateManifestJson
  # base path: http://localhost/api/v1
  # Validates a manifest in json form
  # Validates a manifest in json form
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param component_label character The label of a component in a schema
  # @param restrict_rules character If True, validation suite will only run with in-house validation rule. If False, the Great Expectations suite will be utilized and all rules will be available. (optional)
  # @param body character A manifest in json form (optional)
  # @return [ManifestValidationResult]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})
