# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test ManifestGenerationApi")

api_instance <- ManifestGenerationApi$new()

test_that("GenerateGoogleSheetManifests", {
  # tests for GenerateGoogleSheetManifests
  # base path: http://localhost/api/v1
  # Generates a list of google sheet links
  # Generates a list of google sheet links
  # @param schema_url character The URL of a schema in jsonld or csv form
  # @param add_annotations character If true, annotations are added to the manifest (optional)
  # @param dataset_id_array array[character] An array of dataset ids (optional)
  # @param manifest_title character If making one manifest, the title of the manifest. If making multiple manifests, the prefix of the title of the manifests. (optional)
  # @param data_type_array array[character] An array of data types (optional)
  # @param asset_view_id character ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project (optional)
  # @param use_strict_validation character If true, users are blocked from entering incorrect values. If false, users will get a warning when using incorrect values. (optional)
  # @param generate_all_manifests character If true, a manifest for all components will be generated, datasetIds will be ignored. If false, manifests for each id in datasetIds will be generated. (optional)
  # @return [GoogleSheetLinks]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})
