#' OpenChallenges REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 1.0.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Organization operations
#' @description OrganizationApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetOrganization } \emph{ Get an organization }
#' Returns the organization specified
#'
#' \itemize{
#' \item \emph{ @param } org character
#' \item \emph{ @returnType } \link{Organization} \cr
#'
#'
#' \item status code : 200 | An organization
#'
#' \item return type : Organization
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListOrganizations } \emph{ List organizations }
#' List organizations
#'
#' \itemize{
#' \item \emph{ @param } organization_search_query \link{OrganizationSearchQuery}
#' \item \emph{ @returnType } \link{OrganizationsPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : OrganizationsPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetOrganization  ####################
#'
#' library(openapi)
#' var_org <- "org_example" # character | The unique identifier of the organization.
#'
#' #Get an organization
#' api_instance <- OrganizationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOrganization(var_orgdata_file = "result.txt")
#' result <- api_instance$GetOrganization(var_org)
#' dput(result)
#'
#'
#' ####################  ListOrganizations  ####################
#'
#' library(openapi)
#' var_organization_search_query <- OrganizationSearchQuery$new(123, 123, c(OrganizationCategory$new()), c(ChallengeContributionRole$new()), OrganizationSort$new(), OrganizationDirection$new(), "searchTerms_example") # OrganizationSearchQuery | The search query used to find organizations. (Optional)
#'
#' #List organizations
#' api_instance <- OrganizationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListOrganizations(organization_search_query = var_organization_search_querydata_file = "result.txt")
#' result <- api_instance$ListOrganizations(organization_search_query = var_organization_search_query)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OrganizationApi <- R6::R6Class(
  "OrganizationApi",
  public = list(
    api_client = NULL,
    #' Initialize a new OrganizationApi.
    #'
    #' @description
    #' Initialize a new OrganizationApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Get an organization
    #'
    #' @description
    #' Get an organization
    #'
    #' @param org The unique identifier of the organization.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Organization
    #' @export
    GetOrganization = function(org, data_file = NULL, ...) {
      local_var_response <- self$GetOrganizationWithHttpInfo(org, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get an organization
    #'
    #' @description
    #' Get an organization
    #'
    #' @param org The unique identifier of the organization.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Organization) with additional information such as HTTP status code, headers
    #' @export
    GetOrganizationWithHttpInfo = function(org, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org`)) {
        stop("Missing required parameter `org`.")
      }

      if (nchar(`org`) > 64) {
        stop("Invalid length for `org` when calling OrganizationApi$GetOrganization, must be smaller than or equal to 64.")
      }
      if (nchar(`org`) < 2) {
        stop("Invalid length for `org` when calling OrganizationApi$GetOrganization, must be bigger than or equal to 2.")
      }
      if (!str_detect(`org`, "^[a-z0-9]+(?:-[a-z0-9]+)*$")) {
        stop("Invalid value for `org` when calling OrganizationApi$GetOrganization, must conform to the pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$.")
      }

      local_var_url_path <- "/organizations/{org}"
      if (!missing(`org`)) {
        local_var_url_path <- gsub("\\{org\\}", URLencode(as.character(`org`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "Organization", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List organizations
    #'
    #' @description
    #' List organizations
    #'
    #' @param organization_search_query (optional) The search query used to find organizations.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrganizationsPage
    #' @export
    ListOrganizations = function(organization_search_query = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListOrganizationsWithHttpInfo(organization_search_query, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List organizations
    #'
    #' @description
    #' List organizations
    #'
    #' @param organization_search_query (optional) The search query used to find organizations.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrganizationsPage) with additional information such as HTTP status code, headers
    #' @export
    ListOrganizationsWithHttpInfo = function(organization_search_query = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["organizationSearchQuery"]] <- `organization_search_query`

      local_var_url_path <- "/organizations"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "OrganizationsPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
