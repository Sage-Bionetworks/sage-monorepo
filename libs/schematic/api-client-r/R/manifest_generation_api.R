#' Schematic REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 0.1.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ManifestGeneration operations
#' @description ManifestGenerationApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GenerateGoogleSheetManifests } \emph{ Generates a list of google sheet links }
#' Generates a list of google sheet links
#'
#' \itemize{
#' \item \emph{ @param } schema_url character
#' \item \emph{ @param } add_annotations character
#' \item \emph{ @param } dataset_id_array list( character )
#' \item \emph{ @param } manifest_title character
#' \item \emph{ @param } data_type_array list( character )
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @param } use_strict_validation character
#' \item \emph{ @param } generate_all_manifests character
#' \item \emph{ @returnType } \link{GoogleSheetLinks} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : GoogleSheetLinks
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GenerateGoogleSheetManifests  ####################
#'
#' library(openapi)
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#' var_add_annotations <- FALSE # character | If true, annotations are added to the manifest (Optional)
#' var_dataset_id_array <- c("inner_example") # array[character] | An array of dataset ids (Optional)
#' var_manifest_title <- "manifest_title_example" # character | If making one manifest, the title of the manifest. If making multiple manifests, the prefix of the title of the manifests. (Optional)
#' var_data_type_array <- c("inner_example") # array[character] | An array of data types (Optional)
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project (Optional)
#' var_use_strict_validation <- TRUE # character | If true, users are blocked from entering incorrect values. If false, users will get a warning when using incorrect values. (Optional)
#' var_generate_all_manifests <- FALSE # character | If true, a manifest for all components will be generated, datasetIds will be ignored. If false, manifests for each id in datasetIds will be generated. (Optional)
#'
#' #Generates a list of google sheet links
#' api_instance <- ManifestGenerationApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GenerateGoogleSheetManifests(var_schema_url, add_annotations = var_add_annotations, dataset_id_array = var_dataset_id_array, manifest_title = var_manifest_title, data_type_array = var_data_type_array, asset_view_id = var_asset_view_id, use_strict_validation = var_use_strict_validation, generate_all_manifests = var_generate_all_manifestsdata_file = "result.txt")
#' result <- api_instance$GenerateGoogleSheetManifests(var_schema_url, add_annotations = var_add_annotations, dataset_id_array = var_dataset_id_array, manifest_title = var_manifest_title, data_type_array = var_data_type_array, asset_view_id = var_asset_view_id, use_strict_validation = var_use_strict_validation, generate_all_manifests = var_generate_all_manifests)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ManifestGenerationApi <- R6::R6Class(
  "ManifestGenerationApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ManifestGenerationApi.
    #'
    #' @description
    #' Initialize a new ManifestGenerationApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Generates a list of google sheet links
    #'
    #' @description
    #' Generates a list of google sheet links
    #'
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param add_annotations (optional) If true, annotations are added to the manifest (default value: FALSE)
    #' @param dataset_id_array (optional) An array of dataset ids
    #' @param manifest_title (optional) If making one manifest, the title of the manifest. If making multiple manifests, the prefix of the title of the manifests.
    #' @param data_type_array (optional) An array of data types
    #' @param asset_view_id (optional) ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param use_strict_validation (optional) If true, users are blocked from entering incorrect values. If false, users will get a warning when using incorrect values. (default value: TRUE)
    #' @param generate_all_manifests (optional) If true, a manifest for all components will be generated, datasetIds will be ignored. If false, manifests for each id in datasetIds will be generated. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GoogleSheetLinks
    #' @export
    GenerateGoogleSheetManifests = function(schema_url, add_annotations = FALSE, dataset_id_array = NULL, manifest_title = NULL, data_type_array = NULL, asset_view_id = NULL, use_strict_validation = TRUE, generate_all_manifests = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GenerateGoogleSheetManifestsWithHttpInfo(schema_url, add_annotations, dataset_id_array, manifest_title, data_type_array, asset_view_id, use_strict_validation, generate_all_manifests, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Generates a list of google sheet links
    #'
    #' @description
    #' Generates a list of google sheet links
    #'
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param add_annotations (optional) If true, annotations are added to the manifest (default value: FALSE)
    #' @param dataset_id_array (optional) An array of dataset ids
    #' @param manifest_title (optional) If making one manifest, the title of the manifest. If making multiple manifests, the prefix of the title of the manifests.
    #' @param data_type_array (optional) An array of data types
    #' @param asset_view_id (optional) ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param use_strict_validation (optional) If true, users are blocked from entering incorrect values. If false, users will get a warning when using incorrect values. (default value: TRUE)
    #' @param generate_all_manifests (optional) If true, a manifest for all components will be generated, datasetIds will be ignored. If false, manifests for each id in datasetIds will be generated. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GoogleSheetLinks) with additional information such as HTTP status code, headers
    #' @export
    GenerateGoogleSheetManifestsWithHttpInfo = function(schema_url, add_annotations = FALSE, dataset_id_array = NULL, manifest_title = NULL, data_type_array = NULL, asset_view_id = NULL, use_strict_validation = TRUE, generate_all_manifests = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }









      query_params[["schemaUrl"]] <- `schema_url`

      query_params[["addAnnotations"]] <- `add_annotations`

      # explore
      for (query_item in `dataset_id_array`) {
        query_params[["datasetIdArray"]] <- c(query_params[["datasetIdArray"]], list(`datasetIdArray` = query_item))
      }

      query_params[["manifestTitle"]] <- `manifest_title`

      # explore
      for (query_item in `data_type_array`) {
        query_params[["dataTypeArray"]] <- c(query_params[["dataTypeArray"]], list(`dataTypeArray` = query_item))
      }

      query_params[["assetViewId"]] <- `asset_view_id`

      query_params[["useStrictValidation"]] <- `use_strict_validation`

      query_params[["generateAllManifests"]] <- `generate_all_manifests`

      local_var_url_path <- "/generateGoogleSheetManifests"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "GoogleSheetLinks", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
