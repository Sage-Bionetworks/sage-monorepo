#' OpenChallenges REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 1.0.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Challenge operations
#' @description ChallengeApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetChallenge } \emph{ Get a challenge }
#' Returns the challenge specified
#'
#' \itemize{
#' \item \emph{ @param } challenge_id integer
#' \item \emph{ @returnType } \link{Challenge} \cr
#'
#'
#' \item status code : 200 | A challenge
#'
#' \item return type : Challenge
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListChallenges } \emph{ List challenges }
#' List challenges
#'
#' \itemize{
#' \item \emph{ @param } challenge_search_query \link{ChallengeSearchQuery}
#' \item \emph{ @returnType } \link{ChallengesPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ChallengesPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetChallenge  ####################
#'
#' library(openapi)
#' var_challenge_id <- 56 # integer | The unique identifier of the challenge.
#'
#' #Get a challenge
#' api_instance <- ChallengeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetChallenge(var_challenge_iddata_file = "result.txt")
#' result <- api_instance$GetChallenge(var_challenge_id)
#' dput(result)
#'
#'
#' ####################  ListChallenges  ####################
#'
#' library(openapi)
#' var_challenge_search_query <- ChallengeSearchQuery$new(123, 123, ChallengeSort$new(), ChallengeDirection$new(), c(ChallengeDifficulty$new()), c(ChallengeIncentive$new()), "minStartDate_example", "maxStartDate_example", c("platforms_example"), c(123), c("inputDataTypes_example"), c(ChallengeStatus$new()), c(ChallengeSubmissionType$new()), "searchTerms_example") # ChallengeSearchQuery | The search query used to find challenges. (Optional)
#'
#' #List challenges
#' api_instance <- ChallengeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListChallenges(challenge_search_query = var_challenge_search_querydata_file = "result.txt")
#' result <- api_instance$ListChallenges(challenge_search_query = var_challenge_search_query)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ChallengeApi <- R6::R6Class(
  "ChallengeApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ChallengeApi.
    #'
    #' @description
    #' Initialize a new ChallengeApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Get a challenge
    #'
    #' @description
    #' Get a challenge
    #'
    #' @param challenge_id The unique identifier of the challenge.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Challenge
    #' @export
    GetChallenge = function(challenge_id, data_file = NULL, ...) {
      local_var_response <- self$GetChallengeWithHttpInfo(challenge_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get a challenge
    #'
    #' @description
    #' Get a challenge
    #'
    #' @param challenge_id The unique identifier of the challenge.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Challenge) with additional information such as HTTP status code, headers
    #' @export
    GetChallengeWithHttpInfo = function(challenge_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`challenge_id`)) {
        stop("Missing required parameter `challenge_id`.")
      }


      local_var_url_path <- "/challenges/{challengeId}"
      if (!missing(`challenge_id`)) {
        local_var_url_path <- gsub("\\{challengeId\\}", URLencode(as.character(`challenge_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "Challenge", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List challenges
    #'
    #' @description
    #' List challenges
    #'
    #' @param challenge_search_query (optional) The search query used to find challenges.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ChallengesPage
    #' @export
    ListChallenges = function(challenge_search_query = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListChallengesWithHttpInfo(challenge_search_query, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List challenges
    #'
    #' @description
    #' List challenges
    #'
    #' @param challenge_search_query (optional) The search query used to find challenges.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ChallengesPage) with additional information such as HTTP status code, headers
    #' @export
    ListChallengesWithHttpInfo = function(challenge_search_query = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["challengeSearchQuery"]] <- `challenge_search_query`

      local_var_url_path <- "/challenges"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ChallengesPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
