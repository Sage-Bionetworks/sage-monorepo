#' Schematic REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 0.1.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Schema operations
#' @description SchemaApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetComponent } \emph{ Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String). }
#' Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String).
#'
#' \itemize{
#' \item \emph{ @param } component_label character
#' \item \emph{ @param } schema_url character
#' \item \emph{ @param } include_index character
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetConnectedNodePairArray } \emph{ Gets an array of connected node pairs }
#' Gets a array of connected node pairs
#'
#' \itemize{
#' \item \emph{ @param } schema_url character
#' \item \emph{ @param } relationship_type character
#' \item \emph{ @returnType } \link{ConnectedNodePairArray} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ConnectedNodePairArray
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetConnectedNodePairPage } \emph{ Gets a page of connected node pairs }
#' Gets a page of connected node pairs
#'
#' \itemize{
#' \item \emph{ @param } schema_url character
#' \item \emph{ @param } relationship_type character
#' \item \emph{ @param } page_number integer
#' \item \emph{ @param } page_max_items integer
#' \item \emph{ @returnType } \link{ConnectedNodePairPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ConnectedNodePairPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetNodeDependencyArray } \emph{ Gets the immediate dependencies that are related to the given source node }
#' Gets the immediate dependencies that are related to the given source node
#'
#' \itemize{
#' \item \emph{ @param } node_label character
#' \item \emph{ @param } schema_url character
#' \item \emph{ @param } return_display_names character
#' \item \emph{ @param } return_ordered_by_schema character
#' \item \emph{ @returnType } \link{NodeArray} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : NodeArray
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetNodeDependencyPage } \emph{ Gets the immediate dependencies that are related to the given source node }
#' Gets the immediate dependencies that are related to the given source node
#'
#' \itemize{
#' \item \emph{ @param } node_label character
#' \item \emph{ @param } schema_url character
#' \item \emph{ @param } return_display_names character
#' \item \emph{ @param } return_ordered_by_schema character
#' \item \emph{ @param } page_number integer
#' \item \emph{ @param } page_max_items integer
#' \item \emph{ @returnType } \link{NodePage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : NodePage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetNodeIsRequired } \emph{ Gets whether or not the node is required in the schema }
#' Gets whether or not the node is required in the schema
#'
#' \itemize{
#' \item \emph{ @param } node_display character
#' \item \emph{ @param } schema_url character
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetNodeProperties } \emph{ Gets properties associated with a given node }
#' Gets properties associated with a given node
#'
#' \itemize{
#' \item \emph{ @param } node_label character
#' \item \emph{ @param } schema_url character
#' \item \emph{ @returnType } \link{NodePropertyArray} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : NodePropertyArray
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetNodeValidationRules } \emph{ Gets the validation rules, along with the arguments for each given rule associated with a given node }
#' Gets the validation rules, along with the arguments for each given rule associated with a given node
#'
#' \itemize{
#' \item \emph{ @param } node_display character
#' \item \emph{ @param } schema_url character
#' \item \emph{ @returnType } \link{ValidationRuleArray} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ValidationRuleArray
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetPropertyLabel } \emph{ Gets the property label of the node }
#' Gets the property label of the node
#'
#' \itemize{
#' \item \emph{ @param } node_display character
#' \item \emph{ @param } schema_url character
#' \item \emph{ @param } use_strict_camel_case character
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSchemaAttributes } \emph{ Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String). }
#' Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String).
#'
#' \itemize{
#' \item \emph{ @param } schema_url character
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetComponent  ####################
#'
#' library(openapi)
#' var_component_label <- "component_label_example" # character | The label of a component in a schema
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#' var_include_index <- FALSE # character | Whether to include the indexes of the dataframe in the returned JSON string. (Optional)
#'
#' #Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String).
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetComponent(var_component_label, var_schema_url, include_index = var_include_indexdata_file = "result.txt")
#' result <- api_instance$GetComponent(var_component_label, var_schema_url, include_index = var_include_index)
#' dput(result)
#'
#'
#' ####################  GetConnectedNodePairArray  ####################
#'
#' library(openapi)
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#' var_relationship_type <- "relationship_type_example" # character | Type of relationship in a schema, such as requiresDependency
#'
#' #Gets an array of connected node pairs
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetConnectedNodePairArray(var_schema_url, var_relationship_typedata_file = "result.txt")
#' result <- api_instance$GetConnectedNodePairArray(var_schema_url, var_relationship_type)
#' dput(result)
#'
#'
#' ####################  GetConnectedNodePairPage  ####################
#'
#' library(openapi)
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#' var_relationship_type <- "relationship_type_example" # character | Type of relationship in a schema, such as requiresDependency
#' var_page_number <- 1 # integer | The page number to get for a paginated query (Optional)
#' var_page_max_items <- 100000 # integer | The maximum number of items per page (up to 100,000) for paginated endpoints (Optional)
#'
#' #Gets a page of connected node pairs
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetConnectedNodePairPage(var_schema_url, var_relationship_type, page_number = var_page_number, page_max_items = var_page_max_itemsdata_file = "result.txt")
#' result <- api_instance$GetConnectedNodePairPage(var_schema_url, var_relationship_type, page_number = var_page_number, page_max_items = var_page_max_items)
#' dput(result)
#'
#'
#' ####################  GetNodeDependencyArray  ####################
#'
#' library(openapi)
#' var_node_label <- "node_label_example" # character | The label of the source node in a schema to get the dependencies of
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#' var_return_display_names <- TRUE # character | Whether or not to return the display names of the component, otherwise the label (Optional)
#' var_return_ordered_by_schema <- TRUE # character | Whether or not to order the components by their order in the schema, otherwise random (Optional)
#'
#' #Gets the immediate dependencies that are related to the given source node
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNodeDependencyArray(var_node_label, var_schema_url, return_display_names = var_return_display_names, return_ordered_by_schema = var_return_ordered_by_schemadata_file = "result.txt")
#' result <- api_instance$GetNodeDependencyArray(var_node_label, var_schema_url, return_display_names = var_return_display_names, return_ordered_by_schema = var_return_ordered_by_schema)
#' dput(result)
#'
#'
#' ####################  GetNodeDependencyPage  ####################
#'
#' library(openapi)
#' var_node_label <- "node_label_example" # character | The label of the source node in a schema to get the dependencies of
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#' var_return_display_names <- TRUE # character | Whether or not to return the display names of the component, otherwise the label (Optional)
#' var_return_ordered_by_schema <- TRUE # character | Whether or not to order the components by their order in the schema, otherwise random (Optional)
#' var_page_number <- 1 # integer | The page number to get for a paginated query (Optional)
#' var_page_max_items <- 100000 # integer | The maximum number of items per page (up to 100,000) for paginated endpoints (Optional)
#'
#' #Gets the immediate dependencies that are related to the given source node
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNodeDependencyPage(var_node_label, var_schema_url, return_display_names = var_return_display_names, return_ordered_by_schema = var_return_ordered_by_schema, page_number = var_page_number, page_max_items = var_page_max_itemsdata_file = "result.txt")
#' result <- api_instance$GetNodeDependencyPage(var_node_label, var_schema_url, return_display_names = var_return_display_names, return_ordered_by_schema = var_return_ordered_by_schema, page_number = var_page_number, page_max_items = var_page_max_items)
#' dput(result)
#'
#'
#' ####################  GetNodeIsRequired  ####################
#'
#' library(openapi)
#' var_node_display <- "node_display_example" # character | The display name of the node in a schema
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#'
#' #Gets whether or not the node is required in the schema
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNodeIsRequired(var_node_display, var_schema_urldata_file = "result.txt")
#' result <- api_instance$GetNodeIsRequired(var_node_display, var_schema_url)
#' dput(result)
#'
#'
#' ####################  GetNodeProperties  ####################
#'
#' library(openapi)
#' var_node_label <- "node_label_example" # character | The label of the source node in a schema to get the dependencies of
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#'
#' #Gets properties associated with a given node
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNodeProperties(var_node_label, var_schema_urldata_file = "result.txt")
#' result <- api_instance$GetNodeProperties(var_node_label, var_schema_url)
#' dput(result)
#'
#'
#' ####################  GetNodeValidationRules  ####################
#'
#' library(openapi)
#' var_node_display <- "node_display_example" # character | The display name of the node in a schema
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#'
#' #Gets the validation rules, along with the arguments for each given rule associated with a given node
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNodeValidationRules(var_node_display, var_schema_urldata_file = "result.txt")
#' result <- api_instance$GetNodeValidationRules(var_node_display, var_schema_url)
#' dput(result)
#'
#'
#' ####################  GetPropertyLabel  ####################
#'
#' library(openapi)
#' var_node_display <- "node_display_example" # character | The display name of the node in a schema
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#' var_use_strict_camel_case <- TRUE # character | Whether or not to use the more strict way of converting to camel case (Optional)
#'
#' #Gets the property label of the node
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPropertyLabel(var_node_display, var_schema_url, use_strict_camel_case = var_use_strict_camel_casedata_file = "result.txt")
#' result <- api_instance$GetPropertyLabel(var_node_display, var_schema_url, use_strict_camel_case = var_use_strict_camel_case)
#' dput(result)
#'
#'
#' ####################  GetSchemaAttributes  ####################
#'
#' library(openapi)
#' var_schema_url <- "schema_url_example" # character | The URL of a schema in jsonld or csv form
#'
#' #Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String).
#' api_instance <- SchemaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSchemaAttributes(var_schema_urldata_file = "result.txt")
#' result <- api_instance$GetSchemaAttributes(var_schema_url)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SchemaApi <- R6::R6Class(
  "SchemaApi",
  public = list(
    api_client = NULL,
    #' Initialize a new SchemaApi.
    #'
    #' @description
    #' Initialize a new SchemaApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String).
    #'
    #' @description
    #' Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String).
    #'
    #' @param component_label The label of a component in a schema
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param include_index (optional) Whether to include the indexes of the dataframe in the returned JSON string. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetComponent = function(component_label, schema_url, include_index = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetComponentWithHttpInfo(component_label, schema_url, include_index, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String).
    #'
    #' @description
    #' Get all the attributes associated with a specific data model component formatted as a dataframe (stored as a JSON String).
    #'
    #' @param component_label The label of a component in a schema
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param include_index (optional) Whether to include the indexes of the dataframe in the returned JSON string. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetComponentWithHttpInfo = function(component_label, schema_url, include_index = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`component_label`)) {
        stop("Missing required parameter `component_label`.")
      }

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }




      query_params[["schemaUrl"]] <- `schema_url`

      query_params[["includeIndex"]] <- `include_index`

      local_var_url_path <- "/components/{componentLabel}/"
      if (!missing(`component_label`)) {
        local_var_url_path <- gsub("\\{componentLabel\\}", URLencode(as.character(`component_label`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "character", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets an array of connected node pairs
    #'
    #' @description
    #' Gets an array of connected node pairs
    #'
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param relationship_type Type of relationship in a schema, such as requiresDependency
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ConnectedNodePairArray
    #' @export
    GetConnectedNodePairArray = function(schema_url, relationship_type, data_file = NULL, ...) {
      local_var_response <- self$GetConnectedNodePairArrayWithHttpInfo(schema_url, relationship_type, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets an array of connected node pairs
    #'
    #' @description
    #' Gets an array of connected node pairs
    #'
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param relationship_type Type of relationship in a schema, such as requiresDependency
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ConnectedNodePairArray) with additional information such as HTTP status code, headers
    #' @export
    GetConnectedNodePairArrayWithHttpInfo = function(schema_url, relationship_type, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }

      if (missing(`relationship_type`)) {
        stop("Missing required parameter `relationship_type`.")
      }



      query_params[["schemaUrl"]] <- `schema_url`

      query_params[["relationshipType"]] <- `relationship_type`

      local_var_url_path <- "/connectedNodePairArray"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ConnectedNodePairArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets a page of connected node pairs
    #'
    #' @description
    #' Gets a page of connected node pairs
    #'
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param relationship_type Type of relationship in a schema, such as requiresDependency
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ConnectedNodePairPage
    #' @export
    GetConnectedNodePairPage = function(schema_url, relationship_type, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      local_var_response <- self$GetConnectedNodePairPageWithHttpInfo(schema_url, relationship_type, page_number, page_max_items, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets a page of connected node pairs
    #'
    #' @description
    #' Gets a page of connected node pairs
    #'
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param relationship_type Type of relationship in a schema, such as requiresDependency
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ConnectedNodePairPage) with additional information such as HTTP status code, headers
    #' @export
    GetConnectedNodePairPageWithHttpInfo = function(schema_url, relationship_type, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }

      if (missing(`relationship_type`)) {
        stop("Missing required parameter `relationship_type`.")
      }



      if (`page_number` < 1) {
        stop("Invalid value for `page_number` when calling SchemaApi$GetConnectedNodePairPage, must be bigger than or equal to 1.")
      }

      if (`page_max_items` < 1) {
        stop("Invalid value for `page_max_items` when calling SchemaApi$GetConnectedNodePairPage, must be bigger than or equal to 1.")
      }

      query_params[["schemaUrl"]] <- `schema_url`

      query_params[["relationshipType"]] <- `relationship_type`

      query_params[["pageNumber"]] <- `page_number`

      query_params[["pageMaxItems"]] <- `page_max_items`

      local_var_url_path <- "/connectedNodePairPage"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ConnectedNodePairPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets the immediate dependencies that are related to the given source node
    #'
    #' @description
    #' Gets the immediate dependencies that are related to the given source node
    #'
    #' @param node_label The label of the source node in a schema to get the dependencies of
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param return_display_names (optional) Whether or not to return the display names of the component, otherwise the label (default value: TRUE)
    #' @param return_ordered_by_schema (optional) Whether or not to order the components by their order in the schema, otherwise random (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return NodeArray
    #' @export
    GetNodeDependencyArray = function(node_label, schema_url, return_display_names = TRUE, return_ordered_by_schema = TRUE, data_file = NULL, ...) {
      local_var_response <- self$GetNodeDependencyArrayWithHttpInfo(node_label, schema_url, return_display_names, return_ordered_by_schema, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets the immediate dependencies that are related to the given source node
    #'
    #' @description
    #' Gets the immediate dependencies that are related to the given source node
    #'
    #' @param node_label The label of the source node in a schema to get the dependencies of
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param return_display_names (optional) Whether or not to return the display names of the component, otherwise the label (default value: TRUE)
    #' @param return_ordered_by_schema (optional) Whether or not to order the components by their order in the schema, otherwise random (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (NodeArray) with additional information such as HTTP status code, headers
    #' @export
    GetNodeDependencyArrayWithHttpInfo = function(node_label, schema_url, return_display_names = TRUE, return_ordered_by_schema = TRUE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`node_label`)) {
        stop("Missing required parameter `node_label`.")
      }

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }





      query_params[["schemaUrl"]] <- `schema_url`

      query_params[["returnDisplayNames"]] <- `return_display_names`

      query_params[["returnOrderedBySchema"]] <- `return_ordered_by_schema`

      local_var_url_path <- "/nodes/{nodeLabel}/dependencyArray"
      if (!missing(`node_label`)) {
        local_var_url_path <- gsub("\\{nodeLabel\\}", URLencode(as.character(`node_label`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "NodeArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets the immediate dependencies that are related to the given source node
    #'
    #' @description
    #' Gets the immediate dependencies that are related to the given source node
    #'
    #' @param node_label The label of the source node in a schema to get the dependencies of
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param return_display_names (optional) Whether or not to return the display names of the component, otherwise the label (default value: TRUE)
    #' @param return_ordered_by_schema (optional) Whether or not to order the components by their order in the schema, otherwise random (default value: TRUE)
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return NodePage
    #' @export
    GetNodeDependencyPage = function(node_label, schema_url, return_display_names = TRUE, return_ordered_by_schema = TRUE, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      local_var_response <- self$GetNodeDependencyPageWithHttpInfo(node_label, schema_url, return_display_names, return_ordered_by_schema, page_number, page_max_items, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets the immediate dependencies that are related to the given source node
    #'
    #' @description
    #' Gets the immediate dependencies that are related to the given source node
    #'
    #' @param node_label The label of the source node in a schema to get the dependencies of
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param return_display_names (optional) Whether or not to return the display names of the component, otherwise the label (default value: TRUE)
    #' @param return_ordered_by_schema (optional) Whether or not to order the components by their order in the schema, otherwise random (default value: TRUE)
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (NodePage) with additional information such as HTTP status code, headers
    #' @export
    GetNodeDependencyPageWithHttpInfo = function(node_label, schema_url, return_display_names = TRUE, return_ordered_by_schema = TRUE, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`node_label`)) {
        stop("Missing required parameter `node_label`.")
      }

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }





      if (`page_number` < 1) {
        stop("Invalid value for `page_number` when calling SchemaApi$GetNodeDependencyPage, must be bigger than or equal to 1.")
      }

      if (`page_max_items` < 1) {
        stop("Invalid value for `page_max_items` when calling SchemaApi$GetNodeDependencyPage, must be bigger than or equal to 1.")
      }

      query_params[["schemaUrl"]] <- `schema_url`

      query_params[["returnDisplayNames"]] <- `return_display_names`

      query_params[["returnOrderedBySchema"]] <- `return_ordered_by_schema`

      query_params[["pageNumber"]] <- `page_number`

      query_params[["pageMaxItems"]] <- `page_max_items`

      local_var_url_path <- "/nodes/{nodeLabel}/dependencyPage"
      if (!missing(`node_label`)) {
        local_var_url_path <- gsub("\\{nodeLabel\\}", URLencode(as.character(`node_label`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "NodePage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets whether or not the node is required in the schema
    #'
    #' @description
    #' Gets whether or not the node is required in the schema
    #'
    #' @param node_display The display name of the node in a schema
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetNodeIsRequired = function(node_display, schema_url, data_file = NULL, ...) {
      local_var_response <- self$GetNodeIsRequiredWithHttpInfo(node_display, schema_url, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets whether or not the node is required in the schema
    #'
    #' @description
    #' Gets whether or not the node is required in the schema
    #'
    #' @param node_display The display name of the node in a schema
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetNodeIsRequiredWithHttpInfo = function(node_display, schema_url, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`node_display`)) {
        stop("Missing required parameter `node_display`.")
      }

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }



      query_params[["schemaUrl"]] <- `schema_url`

      local_var_url_path <- "/nodes/{nodeDisplay}/isRequired"
      if (!missing(`node_display`)) {
        local_var_url_path <- gsub("\\{nodeDisplay\\}", URLencode(as.character(`node_display`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "character", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets properties associated with a given node
    #'
    #' @description
    #' Gets properties associated with a given node
    #'
    #' @param node_label The label of the source node in a schema to get the dependencies of
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return NodePropertyArray
    #' @export
    GetNodeProperties = function(node_label, schema_url, data_file = NULL, ...) {
      local_var_response <- self$GetNodePropertiesWithHttpInfo(node_label, schema_url, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets properties associated with a given node
    #'
    #' @description
    #' Gets properties associated with a given node
    #'
    #' @param node_label The label of the source node in a schema to get the dependencies of
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (NodePropertyArray) with additional information such as HTTP status code, headers
    #' @export
    GetNodePropertiesWithHttpInfo = function(node_label, schema_url, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`node_label`)) {
        stop("Missing required parameter `node_label`.")
      }

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }



      query_params[["schemaUrl"]] <- `schema_url`

      local_var_url_path <- "/nodes/{nodeLabel}/nodeProperties"
      if (!missing(`node_label`)) {
        local_var_url_path <- gsub("\\{nodeLabel\\}", URLencode(as.character(`node_label`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "NodePropertyArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets the validation rules, along with the arguments for each given rule associated with a given node
    #'
    #' @description
    #' Gets the validation rules, along with the arguments for each given rule associated with a given node
    #'
    #' @param node_display The display name of the node in a schema
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ValidationRuleArray
    #' @export
    GetNodeValidationRules = function(node_display, schema_url, data_file = NULL, ...) {
      local_var_response <- self$GetNodeValidationRulesWithHttpInfo(node_display, schema_url, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets the validation rules, along with the arguments for each given rule associated with a given node
    #'
    #' @description
    #' Gets the validation rules, along with the arguments for each given rule associated with a given node
    #'
    #' @param node_display The display name of the node in a schema
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ValidationRuleArray) with additional information such as HTTP status code, headers
    #' @export
    GetNodeValidationRulesWithHttpInfo = function(node_display, schema_url, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`node_display`)) {
        stop("Missing required parameter `node_display`.")
      }

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }



      query_params[["schemaUrl"]] <- `schema_url`

      local_var_url_path <- "/nodes/{nodeDisplay}/validationRules"
      if (!missing(`node_display`)) {
        local_var_url_path <- gsub("\\{nodeDisplay\\}", URLencode(as.character(`node_display`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ValidationRuleArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets the property label of the node
    #'
    #' @description
    #' Gets the property label of the node
    #'
    #' @param node_display The display name of the node in a schema
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param use_strict_camel_case (optional) Whether or not to use the more strict way of converting to camel case (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetPropertyLabel = function(node_display, schema_url, use_strict_camel_case = TRUE, data_file = NULL, ...) {
      local_var_response <- self$GetPropertyLabelWithHttpInfo(node_display, schema_url, use_strict_camel_case, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets the property label of the node
    #'
    #' @description
    #' Gets the property label of the node
    #'
    #' @param node_display The display name of the node in a schema
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param use_strict_camel_case (optional) Whether or not to use the more strict way of converting to camel case (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetPropertyLabelWithHttpInfo = function(node_display, schema_url, use_strict_camel_case = TRUE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`node_display`)) {
        stop("Missing required parameter `node_display`.")
      }

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }




      query_params[["schemaUrl"]] <- `schema_url`

      query_params[["useStrictCamelCase"]] <- `use_strict_camel_case`

      local_var_url_path <- "/nodes/{nodeDisplay}/propertyLabel"
      if (!missing(`node_display`)) {
        local_var_url_path <- gsub("\\{nodeDisplay\\}", URLencode(as.character(`node_display`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "character", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String).
    #'
    #' @description
    #' Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String).
    #'
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetSchemaAttributes = function(schema_url, data_file = NULL, ...) {
      local_var_response <- self$GetSchemaAttributesWithHttpInfo(schema_url, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String).
    #'
    #' @description
    #' Get all the attributes associated with a data model formatted as a dataframe (stored as a JSON String).
    #'
    #' @param schema_url The URL of a schema in jsonld or csv form
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetSchemaAttributesWithHttpInfo = function(schema_url, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`schema_url`)) {
        stop("Missing required parameter `schema_url`.")
      }


      query_params[["schemaUrl"]] <- `schema_url`

      local_var_url_path <- "/schemaAttributes"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "character", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
