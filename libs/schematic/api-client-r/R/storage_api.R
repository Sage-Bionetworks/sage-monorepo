#' Schematic REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 0.1.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Storage operations
#' @description StorageApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetAssetViewJson } \emph{ Gets the asset view table in json form }
#' Gets the asset view table in json form
#'
#' \itemize{
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : object
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDatasetFileMetadataArray } \emph{ Gets all files associated with a dataset. }
#' Gets all files associated with a dataset.
#'
#' \itemize{
#' \item \emph{ @param } dataset_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @param } file_names list( character )
#' \item \emph{ @param } use_full_file_path character
#' \item \emph{ @returnType } \link{FileMetadataArray} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : FileMetadataArray
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDatasetFileMetadataPage } \emph{ Gets all files associated with a dataset. }
#' Gets all files associated with a dataset.
#'
#' \itemize{
#' \item \emph{ @param } dataset_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @param } file_names list( character )
#' \item \emph{ @param } use_full_file_path character
#' \item \emph{ @param } page_number integer
#' \item \emph{ @param } page_max_items integer
#' \item \emph{ @returnType } \link{FileMetadataPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : FileMetadataPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDatasetManifestJson } \emph{ Gets the manifest in json form }
#' Gets the manifest in json form
#'
#' \itemize{
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } dataset_id character
#' \item \emph{ @param } asset_view_id character
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : object
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetManifestJson } \emph{ Gets the manifest in json form }
#' Gets the manifest in json form
#'
#' \itemize{
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } manifest_id character
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : object
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectDatasetMetadataArray } \emph{ Gets all dataset metadata in folder under a given storage project that the current user has access to. }
#' Gets all dataset meatdata in folder under a given storage project that the current user has access to.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @returnType } \link{DatasetMetadataArray} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : DatasetMetadataArray
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectDatasetMetadataPage } \emph{ Gets a page of dataset metadata in folder under a given storage project that the current user has access to. }
#' Gets a page of dataset meatdata in folder under a given storage project that the current user has access to.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @param } page_number integer
#' \item \emph{ @param } page_max_items integer
#' \item \emph{ @returnType } \link{DatasetMetadataPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : DatasetMetadataPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectManifestMetadataArray } \emph{ Gets all manifests in a project folder that users have access to }
#' Gets all manifests in a project folder that the current user has access to.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @returnType } \link{ManifestMetadataArray} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ManifestMetadataArray
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectManifestMetadataPage } \emph{ Gets all manifests in a project folder that users have access to }
#' Gets all manifests in a project folder that the current user has access to.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @param } page_number integer
#' \item \emph{ @param } page_max_items integer
#' \item \emph{ @returnType } \link{ManifestMetadataPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ManifestMetadataPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectMetadataArray } \emph{ Gets all storage projects the current user has access to. }
#' Gets all storage projects the current user has access to.
#'
#' \itemize{
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @returnType } \link{ProjectMetadataArray} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ProjectMetadataArray
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectMetadataPage } \emph{ Gets all storage projects the current user has access to. }
#' Gets all storage projects the current user has access to.
#'
#' \itemize{
#' \item \emph{ @param } asset_view_id character
#' \item \emph{ @param } asset_type \link{AssetType}
#' \item \emph{ @param } page_number integer
#' \item \emph{ @param } page_max_items integer
#' \item \emph{ @returnType } \link{ProjectMetadataPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ProjectMetadataPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Unauthorized
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetAssetViewJson  ####################
#'
#' library(openapi)
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#'
#' #Gets the asset view table in json form
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAssetViewJson(var_asset_view_id, var_asset_typedata_file = "result.txt")
#' result <- api_instance$GetAssetViewJson(var_asset_view_id, var_asset_type)
#' dput(result)
#'
#'
#' ####################  GetDatasetFileMetadataArray  ####################
#'
#' library(openapi)
#' var_dataset_id <- "dataset_id_example" # character | The ID of a dataset.
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#' var_file_names <- c("inner_example") # array[character] | A list of file names used to filter the output. (Optional)
#' var_use_full_file_path <- FALSE # character | Whether or not to return the full path of output, or just the basename. (Optional)
#'
#' #Gets all files associated with a dataset.
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDatasetFileMetadataArray(var_dataset_id, var_asset_type, var_asset_view_id, file_names = var_file_names, use_full_file_path = var_use_full_file_pathdata_file = "result.txt")
#' result <- api_instance$GetDatasetFileMetadataArray(var_dataset_id, var_asset_type, var_asset_view_id, file_names = var_file_names, use_full_file_path = var_use_full_file_path)
#' dput(result)
#'
#'
#' ####################  GetDatasetFileMetadataPage  ####################
#'
#' library(openapi)
#' var_dataset_id <- "dataset_id_example" # character | The ID of a dataset.
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#' var_file_names <- c("inner_example") # array[character] | A list of file names used to filter the output. (Optional)
#' var_use_full_file_path <- FALSE # character | Whether or not to return the full path of output, or just the basename. (Optional)
#' var_page_number <- 1 # integer | The page number to get for a paginated query (Optional)
#' var_page_max_items <- 100000 # integer | The maximum number of items per page (up to 100,000) for paginated endpoints (Optional)
#'
#' #Gets all files associated with a dataset.
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDatasetFileMetadataPage(var_dataset_id, var_asset_type, var_asset_view_id, file_names = var_file_names, use_full_file_path = var_use_full_file_path, page_number = var_page_number, page_max_items = var_page_max_itemsdata_file = "result.txt")
#' result <- api_instance$GetDatasetFileMetadataPage(var_dataset_id, var_asset_type, var_asset_view_id, file_names = var_file_names, use_full_file_path = var_use_full_file_path, page_number = var_page_number, page_max_items = var_page_max_items)
#' dput(result)
#'
#'
#' ####################  GetDatasetManifestJson  ####################
#'
#' library(openapi)
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_dataset_id <- "dataset_id_example" # character | The ID of a dataset.
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#'
#' #Gets the manifest in json form
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDatasetManifestJson(var_asset_type, var_dataset_id, var_asset_view_iddata_file = "result.txt")
#' result <- api_instance$GetDatasetManifestJson(var_asset_type, var_dataset_id, var_asset_view_id)
#' dput(result)
#'
#'
#' ####################  GetManifestJson  ####################
#'
#' library(openapi)
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_manifest_id <- "manifest_id_example" # character | ID of a manifest
#'
#' #Gets the manifest in json form
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetManifestJson(var_asset_type, var_manifest_iddata_file = "result.txt")
#' result <- api_instance$GetManifestJson(var_asset_type, var_manifest_id)
#' dput(result)
#'
#'
#' ####################  GetProjectDatasetMetadataArray  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The Synapse ID of a storage project.
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#'
#' #Gets all dataset metadata in folder under a given storage project that the current user has access to.
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectDatasetMetadataArray(var_project_id, var_asset_type, var_asset_view_iddata_file = "result.txt")
#' result <- api_instance$GetProjectDatasetMetadataArray(var_project_id, var_asset_type, var_asset_view_id)
#' dput(result)
#'
#'
#' ####################  GetProjectDatasetMetadataPage  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The Synapse ID of a storage project.
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#' var_page_number <- 1 # integer | The page number to get for a paginated query (Optional)
#' var_page_max_items <- 100000 # integer | The maximum number of items per page (up to 100,000) for paginated endpoints (Optional)
#'
#' #Gets a page of dataset metadata in folder under a given storage project that the current user has access to.
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectDatasetMetadataPage(var_project_id, var_asset_type, var_asset_view_id, page_number = var_page_number, page_max_items = var_page_max_itemsdata_file = "result.txt")
#' result <- api_instance$GetProjectDatasetMetadataPage(var_project_id, var_asset_type, var_asset_view_id, page_number = var_page_number, page_max_items = var_page_max_items)
#' dput(result)
#'
#'
#' ####################  GetProjectManifestMetadataArray  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The Synapse ID of a storage project.
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#'
#' #Gets all manifests in a project folder that users have access to
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectManifestMetadataArray(var_project_id, var_asset_type, var_asset_view_iddata_file = "result.txt")
#' result <- api_instance$GetProjectManifestMetadataArray(var_project_id, var_asset_type, var_asset_view_id)
#' dput(result)
#'
#'
#' ####################  GetProjectManifestMetadataPage  ####################
#'
#' library(openapi)
#' var_project_id <- "project_id_example" # character | The Synapse ID of a storage project.
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#' var_page_number <- 1 # integer | The page number to get for a paginated query (Optional)
#' var_page_max_items <- 100000 # integer | The maximum number of items per page (up to 100,000) for paginated endpoints (Optional)
#'
#' #Gets all manifests in a project folder that users have access to
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectManifestMetadataPage(var_project_id, var_asset_type, var_asset_view_id, page_number = var_page_number, page_max_items = var_page_max_itemsdata_file = "result.txt")
#' result <- api_instance$GetProjectManifestMetadataPage(var_project_id, var_asset_type, var_asset_view_id, page_number = var_page_number, page_max_items = var_page_max_items)
#' dput(result)
#'
#'
#' ####################  GetProjectMetadataArray  ####################
#'
#' library(openapi)
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#'
#' #Gets all storage projects the current user has access to.
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectMetadataArray(var_asset_view_id, var_asset_typedata_file = "result.txt")
#' result <- api_instance$GetProjectMetadataArray(var_asset_view_id, var_asset_type)
#' dput(result)
#'
#'
#' ####################  GetProjectMetadataPage  ####################
#'
#' library(openapi)
#' var_asset_view_id <- "asset_view_id_example" # character | ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
#' var_asset_type <- AssetType$new() # AssetType | Type of asset, such as Synapse
#' var_page_number <- 1 # integer | The page number to get for a paginated query (Optional)
#' var_page_max_items <- 100000 # integer | The maximum number of items per page (up to 100,000) for paginated endpoints (Optional)
#'
#' #Gets all storage projects the current user has access to.
#' api_instance <- StorageApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectMetadataPage(var_asset_view_id, var_asset_type, page_number = var_page_number, page_max_items = var_page_max_itemsdata_file = "result.txt")
#' result <- api_instance$GetProjectMetadataPage(var_asset_view_id, var_asset_type, page_number = var_page_number, page_max_items = var_page_max_items)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
StorageApi <- R6::R6Class(
  "StorageApi",
  public = list(
    api_client = NULL,
    #' Initialize a new StorageApi.
    #'
    #' @description
    #' Initialize a new StorageApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Gets the asset view table in json form
    #'
    #' @description
    #' Gets the asset view table in json form
    #'
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param asset_type Type of asset, such as Synapse
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return object
    #' @export
    GetAssetViewJson = function(asset_view_id, asset_type, data_file = NULL, ...) {
      local_var_response <- self$GetAssetViewJsonWithHttpInfo(asset_view_id, asset_type, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets the asset view table in json form
    #'
    #' @description
    #' Gets the asset view table in json form
    #'
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param asset_type Type of asset, such as Synapse
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (object) with additional information such as HTTP status code, headers
    #' @export
    GetAssetViewJsonWithHttpInfo = function(asset_view_id, asset_type, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }



      local_var_url_path <- "/assetTypes/{assetType}/assetViews/{assetViewId}/json"
      if (!missing(`asset_view_id`)) {
        local_var_url_path <- gsub("\\{assetViewId\\}", URLencode(as.character(`asset_view_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "object", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets all files associated with a dataset.
    #'
    #' @description
    #' Gets all files associated with a dataset.
    #'
    #' @param dataset_id The ID of a dataset.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param file_names (optional) A list of file names used to filter the output.
    #' @param use_full_file_path (optional) Whether or not to return the full path of output, or just the basename. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FileMetadataArray
    #' @export
    GetDatasetFileMetadataArray = function(dataset_id, asset_type, asset_view_id, file_names = NULL, use_full_file_path = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetDatasetFileMetadataArrayWithHttpInfo(dataset_id, asset_type, asset_view_id, file_names, use_full_file_path, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets all files associated with a dataset.
    #'
    #' @description
    #' Gets all files associated with a dataset.
    #'
    #' @param dataset_id The ID of a dataset.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param file_names (optional) A list of file names used to filter the output.
    #' @param use_full_file_path (optional) Whether or not to return the full path of output, or just the basename. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FileMetadataArray) with additional information such as HTTP status code, headers
    #' @export
    GetDatasetFileMetadataArrayWithHttpInfo = function(dataset_id, asset_type, asset_view_id, file_names = NULL, use_full_file_path = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`dataset_id`)) {
        stop("Missing required parameter `dataset_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }






      # explore
      for (query_item in `file_names`) {
        query_params[["fileNames"]] <- c(query_params[["fileNames"]], list(`fileNames` = query_item))
      }

      query_params[["useFullFilePath"]] <- `use_full_file_path`

      query_params[["assetViewId"]] <- `asset_view_id`

      local_var_url_path <- "/assetTypes/{assetType}/datasets/{datasetId}/fileMetadataArray"
      if (!missing(`dataset_id`)) {
        local_var_url_path <- gsub("\\{datasetId\\}", URLencode(as.character(`dataset_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FileMetadataArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets all files associated with a dataset.
    #'
    #' @description
    #' Gets all files associated with a dataset.
    #'
    #' @param dataset_id The ID of a dataset.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param file_names (optional) A list of file names used to filter the output.
    #' @param use_full_file_path (optional) Whether or not to return the full path of output, or just the basename. (default value: FALSE)
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FileMetadataPage
    #' @export
    GetDatasetFileMetadataPage = function(dataset_id, asset_type, asset_view_id, file_names = NULL, use_full_file_path = FALSE, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      local_var_response <- self$GetDatasetFileMetadataPageWithHttpInfo(dataset_id, asset_type, asset_view_id, file_names, use_full_file_path, page_number, page_max_items, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets all files associated with a dataset.
    #'
    #' @description
    #' Gets all files associated with a dataset.
    #'
    #' @param dataset_id The ID of a dataset.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param file_names (optional) A list of file names used to filter the output.
    #' @param use_full_file_path (optional) Whether or not to return the full path of output, or just the basename. (default value: FALSE)
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FileMetadataPage) with additional information such as HTTP status code, headers
    #' @export
    GetDatasetFileMetadataPageWithHttpInfo = function(dataset_id, asset_type, asset_view_id, file_names = NULL, use_full_file_path = FALSE, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`dataset_id`)) {
        stop("Missing required parameter `dataset_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }






      if (`page_number` < 1) {
        stop("Invalid value for `page_number` when calling StorageApi$GetDatasetFileMetadataPage, must be bigger than or equal to 1.")
      }

      if (`page_max_items` < 1) {
        stop("Invalid value for `page_max_items` when calling StorageApi$GetDatasetFileMetadataPage, must be bigger than or equal to 1.")
      }

      # explore
      for (query_item in `file_names`) {
        query_params[["fileNames"]] <- c(query_params[["fileNames"]], list(`fileNames` = query_item))
      }

      query_params[["useFullFilePath"]] <- `use_full_file_path`

      query_params[["assetViewId"]] <- `asset_view_id`

      query_params[["pageNumber"]] <- `page_number`

      query_params[["pageMaxItems"]] <- `page_max_items`

      local_var_url_path <- "/assetTypes/{assetType}/datasets/{datasetId}/fileMetadataPage"
      if (!missing(`dataset_id`)) {
        local_var_url_path <- gsub("\\{datasetId\\}", URLencode(as.character(`dataset_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "FileMetadataPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets the manifest in json form
    #'
    #' @description
    #' Gets the manifest in json form
    #'
    #' @param asset_type Type of asset, such as Synapse
    #' @param dataset_id The ID of a dataset.
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return object
    #' @export
    GetDatasetManifestJson = function(asset_type, dataset_id, asset_view_id, data_file = NULL, ...) {
      local_var_response <- self$GetDatasetManifestJsonWithHttpInfo(asset_type, dataset_id, asset_view_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets the manifest in json form
    #'
    #' @description
    #' Gets the manifest in json form
    #'
    #' @param asset_type Type of asset, such as Synapse
    #' @param dataset_id The ID of a dataset.
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (object) with additional information such as HTTP status code, headers
    #' @export
    GetDatasetManifestJsonWithHttpInfo = function(asset_type, dataset_id, asset_view_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }

      if (missing(`dataset_id`)) {
        stop("Missing required parameter `dataset_id`.")
      }

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }




      query_params[["assetViewId"]] <- `asset_view_id`

      local_var_url_path <- "/assetTypes/{assetType}/datasets/{datasetId}/manifestJson"
      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`dataset_id`)) {
        local_var_url_path <- gsub("\\{datasetId\\}", URLencode(as.character(`dataset_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "object", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets the manifest in json form
    #'
    #' @description
    #' Gets the manifest in json form
    #'
    #' @param asset_type Type of asset, such as Synapse
    #' @param manifest_id ID of a manifest
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return object
    #' @export
    GetManifestJson = function(asset_type, manifest_id, data_file = NULL, ...) {
      local_var_response <- self$GetManifestJsonWithHttpInfo(asset_type, manifest_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets the manifest in json form
    #'
    #' @description
    #' Gets the manifest in json form
    #'
    #' @param asset_type Type of asset, such as Synapse
    #' @param manifest_id ID of a manifest
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (object) with additional information such as HTTP status code, headers
    #' @export
    GetManifestJsonWithHttpInfo = function(asset_type, manifest_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }

      if (missing(`manifest_id`)) {
        stop("Missing required parameter `manifest_id`.")
      }



      local_var_url_path <- "/assetTypes/{assetType}/manifests/{manifestId}/json"
      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`manifest_id`)) {
        local_var_url_path <- gsub("\\{manifestId\\}", URLencode(as.character(`manifest_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "object", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets all dataset metadata in folder under a given storage project that the current user has access to.
    #'
    #' @description
    #' Gets all dataset metadata in folder under a given storage project that the current user has access to.
    #'
    #' @param project_id The Synapse ID of a storage project.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return DatasetMetadataArray
    #' @export
    GetProjectDatasetMetadataArray = function(project_id, asset_type, asset_view_id, data_file = NULL, ...) {
      local_var_response <- self$GetProjectDatasetMetadataArrayWithHttpInfo(project_id, asset_type, asset_view_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets all dataset metadata in folder under a given storage project that the current user has access to.
    #'
    #' @description
    #' Gets all dataset metadata in folder under a given storage project that the current user has access to.
    #'
    #' @param project_id The Synapse ID of a storage project.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (DatasetMetadataArray) with additional information such as HTTP status code, headers
    #' @export
    GetProjectDatasetMetadataArrayWithHttpInfo = function(project_id, asset_type, asset_view_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }




      query_params[["assetViewId"]] <- `asset_view_id`

      local_var_url_path <- "/assetTypes/{assetType}/projects/{projectId}/datasetMetadataArray"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "DatasetMetadataArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets a page of dataset metadata in folder under a given storage project that the current user has access to.
    #'
    #' @description
    #' Gets a page of dataset metadata in folder under a given storage project that the current user has access to.
    #'
    #' @param project_id The Synapse ID of a storage project.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return DatasetMetadataPage
    #' @export
    GetProjectDatasetMetadataPage = function(project_id, asset_type, asset_view_id, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      local_var_response <- self$GetProjectDatasetMetadataPageWithHttpInfo(project_id, asset_type, asset_view_id, page_number, page_max_items, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets a page of dataset metadata in folder under a given storage project that the current user has access to.
    #'
    #' @description
    #' Gets a page of dataset metadata in folder under a given storage project that the current user has access to.
    #'
    #' @param project_id The Synapse ID of a storage project.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (DatasetMetadataPage) with additional information such as HTTP status code, headers
    #' @export
    GetProjectDatasetMetadataPageWithHttpInfo = function(project_id, asset_type, asset_view_id, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }




      if (`page_number` < 1) {
        stop("Invalid value for `page_number` when calling StorageApi$GetProjectDatasetMetadataPage, must be bigger than or equal to 1.")
      }

      if (`page_max_items` < 1) {
        stop("Invalid value for `page_max_items` when calling StorageApi$GetProjectDatasetMetadataPage, must be bigger than or equal to 1.")
      }

      query_params[["assetViewId"]] <- `asset_view_id`

      query_params[["pageNumber"]] <- `page_number`

      query_params[["pageMaxItems"]] <- `page_max_items`

      local_var_url_path <- "/assetTypes/{assetType}/projects/{projectId}/datasetMetadataPage"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "DatasetMetadataPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets all manifests in a project folder that users have access to
    #'
    #' @description
    #' Gets all manifests in a project folder that users have access to
    #'
    #' @param project_id The Synapse ID of a storage project.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ManifestMetadataArray
    #' @export
    GetProjectManifestMetadataArray = function(project_id, asset_type, asset_view_id, data_file = NULL, ...) {
      local_var_response <- self$GetProjectManifestMetadataArrayWithHttpInfo(project_id, asset_type, asset_view_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets all manifests in a project folder that users have access to
    #'
    #' @description
    #' Gets all manifests in a project folder that users have access to
    #'
    #' @param project_id The Synapse ID of a storage project.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ManifestMetadataArray) with additional information such as HTTP status code, headers
    #' @export
    GetProjectManifestMetadataArrayWithHttpInfo = function(project_id, asset_type, asset_view_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }




      query_params[["assetViewId"]] <- `asset_view_id`

      local_var_url_path <- "/assetTypes/{assetType}/projects/{projectId}/manifestMetadataArray"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ManifestMetadataArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets all manifests in a project folder that users have access to
    #'
    #' @description
    #' Gets all manifests in a project folder that users have access to
    #'
    #' @param project_id The Synapse ID of a storage project.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ManifestMetadataPage
    #' @export
    GetProjectManifestMetadataPage = function(project_id, asset_type, asset_view_id, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      local_var_response <- self$GetProjectManifestMetadataPageWithHttpInfo(project_id, asset_type, asset_view_id, page_number, page_max_items, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets all manifests in a project folder that users have access to
    #'
    #' @description
    #' Gets all manifests in a project folder that users have access to
    #'
    #' @param project_id The Synapse ID of a storage project.
    #' @param asset_type Type of asset, such as Synapse
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ManifestMetadataPage) with additional information such as HTTP status code, headers
    #' @export
    GetProjectManifestMetadataPageWithHttpInfo = function(project_id, asset_type, asset_view_id, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }




      if (`page_number` < 1) {
        stop("Invalid value for `page_number` when calling StorageApi$GetProjectManifestMetadataPage, must be bigger than or equal to 1.")
      }

      if (`page_max_items` < 1) {
        stop("Invalid value for `page_max_items` when calling StorageApi$GetProjectManifestMetadataPage, must be bigger than or equal to 1.")
      }

      query_params[["assetViewId"]] <- `asset_view_id`

      query_params[["pageNumber"]] <- `page_number`

      query_params[["pageMaxItems"]] <- `page_max_items`

      local_var_url_path <- "/assetTypes/{assetType}/projects/{projectId}/manifestMetadataPage"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ManifestMetadataPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets all storage projects the current user has access to.
    #'
    #' @description
    #' Gets all storage projects the current user has access to.
    #'
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param asset_type Type of asset, such as Synapse
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectMetadataArray
    #' @export
    GetProjectMetadataArray = function(asset_view_id, asset_type, data_file = NULL, ...) {
      local_var_response <- self$GetProjectMetadataArrayWithHttpInfo(asset_view_id, asset_type, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets all storage projects the current user has access to.
    #'
    #' @description
    #' Gets all storage projects the current user has access to.
    #'
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param asset_type Type of asset, such as Synapse
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectMetadataArray) with additional information such as HTTP status code, headers
    #' @export
    GetProjectMetadataArrayWithHttpInfo = function(asset_view_id, asset_type, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }



      local_var_url_path <- "/assetTypes/{assetType}/assetViews/{assetViewId}/projectMetadataArray"
      if (!missing(`asset_view_id`)) {
        local_var_url_path <- gsub("\\{assetViewId\\}", URLencode(as.character(`asset_view_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ProjectMetadataArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gets all storage projects the current user has access to.
    #'
    #' @description
    #' Gets all storage projects the current user has access to.
    #'
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param asset_type Type of asset, such as Synapse
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectMetadataPage
    #' @export
    GetProjectMetadataPage = function(asset_view_id, asset_type, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      local_var_response <- self$GetProjectMetadataPageWithHttpInfo(asset_view_id, asset_type, page_number, page_max_items, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gets all storage projects the current user has access to.
    #'
    #' @description
    #' Gets all storage projects the current user has access to.
    #'
    #' @param asset_view_id ID of view listing all project data assets. E.g. for Synapse this would be the Synapse ID of the fileview listing all data assets for a given project
    #' @param asset_type Type of asset, such as Synapse
    #' @param page_number (optional) The page number to get for a paginated query (default value: 1)
    #' @param page_max_items (optional) The maximum number of items per page (up to 100,000) for paginated endpoints (default value: 100000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectMetadataPage) with additional information such as HTTP status code, headers
    #' @export
    GetProjectMetadataPageWithHttpInfo = function(asset_view_id, asset_type, page_number = 1, page_max_items = 100000, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_view_id`)) {
        stop("Missing required parameter `asset_view_id`.")
      }

      if (missing(`asset_type`)) {
        stop("Missing required parameter `asset_type`.")
      }



      if (`page_number` < 1) {
        stop("Invalid value for `page_number` when calling StorageApi$GetProjectMetadataPage, must be bigger than or equal to 1.")
      }

      if (`page_max_items` < 1) {
        stop("Invalid value for `page_max_items` when calling StorageApi$GetProjectMetadataPage, must be bigger than or equal to 1.")
      }

      query_params[["pageNumber"]] <- `page_number`

      query_params[["pageMaxItems"]] <- `page_max_items`

      local_var_url_path <- "/assetTypes/{assetType}/assetViews/{assetViewId}/projectMetadataPage"
      if (!missing(`asset_view_id`)) {
        local_var_url_path <- gsub("\\{assetViewId\\}", URLencode(as.character(`asset_view_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_type`)) {
        local_var_url_path <- gsub("\\{assetType\\}", URLencode(as.character(`asset_type`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ProjectMetadataPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
