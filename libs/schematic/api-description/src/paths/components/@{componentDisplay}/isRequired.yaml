parameters:
  - $ref: ../../../components/parameters/path/componentDisplay.yaml
get:
  tags:
    - Schema
  summary: Gets whether or not the component is required in the schema
  description: Gets whether or not the component is required in the schema
  operationId: componentIsRequired
  parameters:
    - $ref: ../../../components/parameters/query/schemaUrl.yaml
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            description: Whether or not the component is required in the schema
            type: boolean
    '500':
      $ref: ../../../components/responses/InternalServerError.yaml
