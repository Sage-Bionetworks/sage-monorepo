[tool.coverage.run]
branch = true
source = [ "sandbox_py_lib_a" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-fail-under=100
 --cov-report html:'../../../coverage/libs/sandbox/py-lib-a/html'
 --cov-report xml:'../../../coverage/libs/sandbox/py-lib-a/coverage.xml'
 --html='../../../reports/libs/sandbox/py-lib-a/unittests/html/index.html'
 --junitxml='../../../reports/libs/sandbox/py-lib-a/unittests/junit.xml'
"""

[project]
name = "sandbox-py-lib-a"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.9,<4"
readme = 'README.md'
dependencies = [
    "sandbox-py-lib-b",
]

[tool.hatch.build.targets.wheel]
packages = ["sandbox_py_lib_a"]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []

[tool.uv.sources]
sandbox-py-lib-b = { workspace = true }
