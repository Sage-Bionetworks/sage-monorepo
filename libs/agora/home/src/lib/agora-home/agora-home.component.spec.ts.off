import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgoraHomeComponent } from './agora-home.component';

describe('AgoraHomeComponent', () => {
  let component: AgoraHomeComponent;
  let fixture: ComponentFixture<AgoraHomeComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AgoraHomeComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(AgoraHomeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
