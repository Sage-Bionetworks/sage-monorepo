import { Routes } from '@angular/router';
import { NominationFormComponent } from './nomination-form.component';

export const nominationFormRoute: Routes = [{ path: '', component: NominationFormComponent }];
