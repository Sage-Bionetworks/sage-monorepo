openapi: 3.0.3
info:
  version: 1.0.0
  title: Agora API
  license:
    name: Apache 2.0
    url: https://github.com/Sage-Bionetworks/sage-monorepo/blob/main/LICENSE.txt
  contact:
    name: Support
    url: https://github.com/Sage-Bionetworks/sage-monorepo
  x-logo:
    url: https://raw.githubusercontent.com/Sage-Bionetworks/agora-images/main/og-social.png
    altText: Agora
servers:
  - url: http://localhost/v1
tags:
  - name: Bio Domain
    description: Operations about bio domains.
  - name: Data Version
    description: Operations about the data version.
  - name: Distribution
    description: Operations about distributions.
  - name: Gene
    description: Operations about genes.
  - name: Nominated Gene
    description: Operations about nominated genes.
  - name: Team
    description: Operations about teams.
paths:
  /bio-domains:
    $ref: paths/bio-domains.yaml
  /bio-domains/{ensg}:
    $ref: paths/bio-domains/{ensg}.yaml
  /genes:
    $ref: paths/genes.yaml
  /genes/{ensg}:
    $ref: paths/genes/{ensg}.yaml
  /genes/search:
    $ref: paths/genes/search.yaml
  /genes/search/enhanced:
    $ref: paths/genes/search-enhanced.yaml
  /genes/comparison:
    $ref: paths/genes/comparison.yaml
  /genes/nominated:
    $ref: paths/genes/nominated.yaml
  /data-version:
    $ref: paths/data-version.yaml
  /distribution:
    $ref: paths/distribution.yaml
  /teams:
    $ref: paths/teams.yaml
  /team-members/{name}/image:
    $ref: paths/team-members/{name}/image.yaml
