type: object
description: A page of differential expression profiles (mRNA).
allOf:
  - $ref: PageMetadata.yaml
  - type: object
    properties:
      differentialExpressionProfilesMRna:
        description: A list of differential epxression profiles (mRNA).
        type: array
        items:
          $ref: DifferentialExpressionProfileMRna.yaml
    required:
      - differentialExpressionProfilesMRna
