type: object
description: Search result
properties:
  id:
    type: string
    description: ID of result
  match_field:
    type: string
    description: Field that matched the query
  match_value:
    type: string
    description: Value that matched the query
  hgnc_symbol:
    type: string
    description: HGNC symbol of the gene
required:
  - id
  - match_field
  - match_value
