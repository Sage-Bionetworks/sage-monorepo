type: object
description: A differential expression profile search query (RNA).
properties:
  pageNumber:
    description: The page number.
    type: integer
    format: int32
    default: 0
    minimum: 0
  pageSize:
    description: The number of items in a single page.
    type: integer
    format: int32
    default: 100
    minimum: 1
    maximum: 100
  sort:
    $ref: DifferentialExpressionProfileRnaSort.yaml
  direction:
    $ref: SortDirection.yaml
  model:
    $ref: DifferentialExpressionProfileRnaModel.yaml
  searchTerms:
    description: A string of search terms used to filter the results.
    type: string
    example: 'A1BG'
