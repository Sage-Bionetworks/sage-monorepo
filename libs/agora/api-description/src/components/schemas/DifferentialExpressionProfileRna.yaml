type: object
description: A differential expression profile (RNA).
properties:
  ensembl_gene_id:
    $ref: EnsemblGeneId.yaml
  hgnc_symbol:
    $ref: HgncGeneSymbol.yaml
  target_risk_score:
    type: number
    nullable: true
    description: 'Target risk score'
required:
  - ensembl_gene_id
