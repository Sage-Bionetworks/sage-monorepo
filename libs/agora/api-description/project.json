{
  "name": "agora-api-description",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/agora/api-description/src",
  "projectType": "library",
  "targets": {
    "build-individuals": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "redocly bundle --output openapi/base.openapi.yaml src/base.openapi.yaml",
          "redocly bundle --output openapi/main.openapi.yaml src/main.openapi.yaml",
          "redocly bundle --output openapi/cgt-v2beta.openapi.yaml src/cgt-v2beta.openapi.yaml"
        ],
        "parallel": true,
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "openapi-merge-cli --config openapi-merge.yaml",
          "rm openapi/base.openapi.yaml"
        ],
        "cwd": "{projectRoot}",
        "parallel": false
      },
      "dependsOn": ["build-individuals"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "redocly lint --config tools/redocly/config.yaml {projectName}"
      },
      "dependsOn": ["build"]
    }
  },
  "tags": ["language:openapi"]
}
