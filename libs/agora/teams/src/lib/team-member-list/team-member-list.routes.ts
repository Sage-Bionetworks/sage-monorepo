import { Routes } from '@angular/router';
import { TeamMemberListComponent } from './team-member-list.component';

export const teamMemberListRoutes: Routes = [{ path: '', component: TeamMemberListComponent }];
