openapi: 3.0.3
info:
  version: 1.0.0
  title: Model-AD API Next
  license:
    name: Apache 2.0
    url: https://github.com/Sage-Bionetworks/sage-monorepo/blob/main/LICENSE.txt
  contact:
    name: Support
    url: https://github.com/Sage-Bionetworks/sage-monorepo
  x-logo:
    url: https://dev.modeladexplorer.org/opengraph/opengraph.png
    altText: Model-AD Explorer
servers:
  - url: http://localhost/v1
tags:
  - name: Model Overview
    description: Operations about Model Overview.
  - name: Disease Correlation
    description: Operations about Disease Correlation results.
  - name: Gene Expression
    description: Operations about Gene Expression results.
paths:
  /comparison-tools/model-overview:
    get:
      tags:
        - Model Overview
      summary: Get model overview for comparison tools
      description: Returns a paginated list of model overview objects for use in comparison tools.
      operationId: getModelOverviews
      parameters:
        - $ref: '#/components/parameters/modelOverviewSearchQuery'
      responses:
        '200':
          description: A paginated list of model overview objects
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelOverviewsPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /comparison-tools/disease-correlation:
    get:
      tags:
        - Disease Correlation
      summary: Get disease correlation comparison data
      description: Returns a paginated list of disease correlation objects for use in comparison tools.
      operationId: getDiseaseCorrelations
      parameters:
        - $ref: '#/components/parameters/diseaseCorrelationSearchQuery'
      responses:
        '200':
          description: A paginated response containing disease correlation objects
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiseaseCorrelationsPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /comparison-tools/gene-expression:
    get:
      tags:
        - Gene Expression
      summary: Get gene expression comparison data
      description: Returns a paginated list of gene expression objects for use in comparison tools.
      operationId: getGeneExpressions
      parameters:
        - $ref: '#/components/parameters/geneExpressionSearchQuery'
      responses:
        '200':
          description: A paginated response containing gene expression objects
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GeneExpressionsPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
components:
  schemas:
    ItemFilterTypeQuery:
      type: string
      enum:
        - include
        - exclude
      description: |
        - include: Include only the specified items.
        - exclude: Exclude the specified items.
      default: include
    ModelOverviewSearchQuery:
      type: object
      description: Model overview search query with pagination and filtering options.
      properties:
        pageNumber:
          description: The page number to return (index starts from 0).
          type: integer
          format: int32
          default: 0
          minimum: 0
          example: 0
        pageSize:
          description: The number of items in a single page.
          type: integer
          format: int32
          default: 100
          minimum: 1
          maximum: 100
          example: 100
        items:
          description: |
            List of item names to filter by.
          type: array
          items:
            type: string
          nullable: true
          example:
            - 3xTg-AD
            - 5xFAD (UCI)
        itemFilterType:
          $ref: '#/components/schemas/ItemFilterTypeQuery'
          nullable: true
        search:
          description: |
            Search by model name (case-insensitive partial match) or by comma separated list of model names (case-insensitive full matches).
            Examples: '3xtg-ad,5xfad' (comma-separated list) or 'fad' (partial match). Only applied when
            itemFilterType is 'exclude'.
          type: string
          nullable: true
          example: 3xtg-ad,5xfad
        sortFields:
          description: |
            List of field names to sort by (e.g., ["model_type", "name"]).
            Each field in sortFields must have a corresponding order in sortOrders.
          type: array
          items:
            type: string
          nullable: true
          example:
            - model_type
            - name
        sortOrders:
          description: |
            List of sort directions corresponding to sortFields.
            Values: 1 (ascending) or -1 (descending).
            Must have the same length as sortFields.
          type: array
          items:
            type: integer
            format: int32
          nullable: true
          example:
            - -1
            - 1
      required:
        - sortFields
        - sortOrders
    ModelOverviewLink:
      type: object
      description: Model Overview Link
      properties:
        link_text:
          type: string
          description: Link text
        link_url:
          type: string
          description: URL for the related resource
    ModelOverview:
      type: object
      description: Model Overview object for comparison tools
      properties:
        _id:
          type: string
          description: Unique identifier for the model overview object
        name:
          type: string
          description: Model name
        model_type:
          type: string
          description: Model type (e.g., Late Onset AD, Familial AD)
        matched_controls:
          type: array
          items:
            type: string
          description: List of matched control models
        gene_expression:
          $ref: '#/components/schemas/ModelOverviewLink'
          nullable: true
        disease_correlation:
          $ref: '#/components/schemas/ModelOverviewLink'
          nullable: true
        biomarkers:
          $ref: '#/components/schemas/ModelOverviewLink'
          nullable: true
        pathology:
          $ref: '#/components/schemas/ModelOverviewLink'
          nullable: true
        study_data:
          $ref: '#/components/schemas/ModelOverviewLink'
          nullable: true
        jax_strain:
          $ref: '#/components/schemas/ModelOverviewLink'
          nullable: true
        center:
          $ref: '#/components/schemas/ModelOverviewLink'
          nullable: true
        modified_genes:
          type: array
          items:
            type: string
          description: List of modified genes in the model
        available_data:
          type: array
          items:
            type: string
            enum:
              - Gene Expression
              - Pathology
              - Biomarkers
              - Disease Correlation
      required:
        - _id
        - name
        - model_type
        - matched_controls
        - study_data
        - jax_strain
        - center
        - modified_genes
        - available_data
    PageMetadata:
      type: object
      description: The metadata of a page.
      properties:
        number:
          type: integer
          description: The page number.
          example: 0
        size:
          type: integer
          description: The number of items in a single page.
          example: 100
        totalElements:
          type: integer
          format: int64
          description: Total number of elements in the result set.
          example: 250
        totalPages:
          type: integer
          description: Total number of pages in the result set.
          example: 3
        hasNext:
          type: boolean
          description: Returns if there is a next page.
          example: true
        hasPrevious:
          type: boolean
          description: Returns if there is a previous page.
          example: false
      required:
        - number
        - size
        - totalElements
        - totalPages
        - hasNext
        - hasPrevious
    ModelOverviewsPage:
      type: object
      description: A page of model overview objects.
      properties:
        modelOverviews:
          type: array
          description: List of model overviews in this page.
          items:
            $ref: '#/components/schemas/ModelOverview'
        page:
          $ref: '#/components/schemas/PageMetadata'
      required:
        - modelOverviews
        - page
    BasicError:
      type: object
      description: Problem details (tools.ietf.org/html/rfc7807)
      properties:
        title:
          type: string
          description: A human readable documentation for the problem type
        status:
          type: integer
          description: The HTTP status code
        detail:
          type: string
          description: A human readable explanation specific to this occurrence of the problem
        type:
          type: string
          description: An absolute URI that identifies the problem type
        instance:
          type: string
          description: An absolute URI that identifies the specific occurrence of the problem
      required:
        - title
        - status
    DiseaseCorrelationSearchQuery:
      type: object
      description: Disease correlation search query with pagination and filtering options.
      properties:
        pageNumber:
          description: The page number.
          type: integer
          format: int32
          default: 0
          minimum: 0
          example: 0
        pageSize:
          description: The number of items in a single page.
          type: integer
          format: int32
          default: 100
          minimum: 1
          maximum: 100
          example: 100
        categories:
          description: |
            Array of category values from the dropdown selections.
            The API will parse these to extract the cluster information.
            Expected format: [mainCategory, clusterCategory]
          type: array
          items:
            type: string
          minItems: 2
          maxItems: 2
          example:
            - CONSENSUS NETWORK MODULES
            - Consensus Cluster A - ECM Organization
        items:
          description: |
            List of composite identifiers to filter by. Each identifier uses the format "name~age~sex"
            where each identifier represents one complete combination of model name, age, and sex.

            Example: "APOE4~4 months~Female" filters for documents matching that exact combination.
            Multiple items can be provided to filter for multiple specific combinations.
          type: array
          items:
            type: string
          nullable: true
          example:
            - APOE4~4 months~Female
            - APOE4~8 months~Male
            - 5xFAD (IU/Jax/Pitt)~12 months~Female
        itemFilterType:
          $ref: '#/components/schemas/ItemFilterTypeQuery'
          nullable: true
        search:
          description: |
            Search by model name (case-insensitive partial match) or by comma separated list of model names (case-insensitive full matches).
            Examples: '3xtg-ad,5xfad (uci)' (comma-separated list) or 'fad' (partial match). Only applied when
            itemFilterType is 'exclude'.
          type: string
          nullable: true
          example: 3xtg-ad,5xfad (uci)
        sortFields:
          description: |
            List of field names to sort by (e.g., ["name", "age", "sex"]).
            Each field in sortFields must have a corresponding order in sortOrders.
          type: array
          items:
            type: string
          nullable: true
          example:
            - name
            - age
            - sex
        sortOrders:
          description: |
            List of sort directions corresponding to sortFields.
            Values: 1 (ascending) or -1 (descending).
            Must have the same length as sortFields.
          type: array
          items:
            type: integer
            format: int32
          nullable: true
          example:
            - 1
            - -1
            - 1
      required:
        - categories
        - sortFields
        - sortOrders
    Sex:
      type: string
      description: Sex
      enum:
        - Female
        - Male
    CorrelationResult:
      type: object
      description: Correlation result
      properties:
        correlation:
          type: number
          description: Correlation value
        adj_p_val:
          type: number
          description: Adjusted p-value
      required:
        - correlation
        - adj_p_val
    DiseaseCorrelation:
      type: object
      description: Disease Correlation
      properties:
        composite_id:
          type: string
          description: Unique identifier for the disease correlation object
          example: APOE4~12 Months~Female
        name:
          type: string
          description: Model name
        matched_control:
          type: string
          description: Matched control for the model
        model_type:
          type: string
          description: Type of model
        modified_genes:
          type: array
          items:
            type: string
          description: List of modified genes
        cluster:
          type: string
          description: Cluster information
        age:
          type: string
          description: Age of the model
        sex:
          $ref: '#/components/schemas/Sex'
        IFG:
          $ref: '#/components/schemas/CorrelationResult'
        PHG:
          $ref: '#/components/schemas/CorrelationResult'
        TCX:
          $ref: '#/components/schemas/CorrelationResult'
        CBE:
          $ref: '#/components/schemas/CorrelationResult'
        DLPFC:
          $ref: '#/components/schemas/CorrelationResult'
        FP:
          $ref: '#/components/schemas/CorrelationResult'
        STG:
          $ref: '#/components/schemas/CorrelationResult'
      required:
        - composite_id
        - name
        - matched_control
        - model_type
        - modified_genes
        - cluster
        - age
        - sex
    DiseaseCorrelationsPage:
      type: object
      description: A paginated response containing disease correlation objects and pagination metadata.
      properties:
        diseaseCorrelations:
          type: array
          description: The list of disease correlation objects for the current page
          items:
            $ref: '#/components/schemas/DiseaseCorrelation'
        page:
          $ref: '#/components/schemas/PageMetadata'
      required:
        - diseaseCorrelations
        - page
    GeneExpressionSearchQuery:
      type: object
      description: Gene expression search query with pagination and filtering options.
      properties:
        pageNumber:
          description: The page number.
          type: integer
          format: int32
          default: 0
          minimum: 0
          example: 0
        pageSize:
          description: The number of items in a single page.
          type: integer
          format: int32
          default: 100
          minimum: 1
          maximum: 100
          example: 100
        categories:
          description: |
            Array of category values from the dropdown selections.
            The API will parse these to extract the tissue and sex_cohort information.
            Expected format: [mainCategory, tissueCategory, sexCohortCategory]
          type: array
          items:
            type: string
          minItems: 3
          maxItems: 3
          example:
            - RNA - DIFFERENTIAL EXPRESSION
            - Tissue - Hemibrain
            - Sex - Females & Males
        items:
          description: |
            List of composite identifiers to filter by. Each identifier uses the format "ensembl_gene_id~name"
            where each identifier represents one complete combination of ensembl gene ID and gene name.

            Example: "ENSMUSG00000000001~5xFAD (Jax/IU/Pitt)" filters for documents matching that exact gene ID and name.
            Multiple items can be provided to filter for multiple specific combinations.
          type: array
          items:
            type: string
          nullable: true
          example:
            - ENSMUSG00000000001~5xFAD (Jax/IU/Pitt)
            - ENSMUSG00000000028~APOE4
        itemFilterType:
          $ref: '#/components/schemas/ItemFilterTypeQuery'
          nullable: true
        search:
          description: |
            Search by gene symbol (case-insensitive partial match) or by comma separated list of gene symbols (case-insensitive full matches).
            Examples: 'gnai,cdc45' (comma-separated list) or 'gna' (partial match). Only applied when
            itemFilterType is 'exclude'.
          type: string
          nullable: true
          example: gnai,cdc45
        sortFields:
          description: |
            List of field names to sort by (e.g., "gene_symbol,name").
            Each field in sortFields must have a corresponding order in sortOrders.
          type: array
          items:
            type: string
          nullable: true
          example:
            - gene_symbol
            - name
        sortOrders:
          description: |
            List of sort directions corresponding to sortFields.
            Values: 1 (ascending) or -1 (descending).
            Must have the same length as sortFields.
          type: array
          items:
            type: integer
            format: int32
          nullable: true
          example:
            - 1
            - -1
      required:
        - categories
        - sortFields
        - sortOrders
    SexCohort:
      type: string
      description: Sex Cohort
      enum:
        - Females
        - Females & Males
        - Males
    FoldChangeResult:
      type: object
      description: Fold Change result
      properties:
        log2_fc:
          type: number
          description: Log2 fold change
        adj_p_val:
          type: number
          description: Adjusted p-value
      required:
        - log2_fc
        - adj_p_val
    GeneExpression:
      type: object
      description: Gene Expression
      properties:
        composite_id:
          type: string
          description: Unique identifier for the gene expression object
          example: ENSMUSG00000000001~5xFAD (Jax/IU/Pitt)
        ensembl_gene_id:
          type: string
          description: Ensembl Gene ID
        gene_symbol:
          type: string
          description: Gene Symbol
          example: Gnai3
        biodomains:
          type: array
          description: List of biodomains associated with the gene
          items:
            type: string
        name:
          type: string
          description: Model name
        matched_control:
          type: string
          description: Matched control for the model
          example: C57BL/6J
        model_group:
          type: string
          description: Model group
          nullable: true
        model_type:
          type: string
          description: Type of model
          example: Familial AD
        tissue:
          type: string
          description: Tissue type
          example: Hemibrain
        sex_cohort:
          $ref: '#/components/schemas/SexCohort'
        4 months:
          $ref: '#/components/schemas/FoldChangeResult'
        12 months:
          $ref: '#/components/schemas/FoldChangeResult'
        18 months:
          $ref: '#/components/schemas/FoldChangeResult'
      required:
        - composite_id
        - ensembl_gene_id
        - gene_symbol
        - biodomains
        - name
        - matched_control
        - model_group
        - model_type
        - tissue
        - sex_cohort
    GeneExpressionsPage:
      type: object
      description: A paginated response containing gene expression objects and pagination metadata.
      properties:
        geneExpressions:
          type: array
          description: The list of gene expression objects for the current page
          items:
            $ref: '#/components/schemas/GeneExpression'
        page:
          $ref: '#/components/schemas/PageMetadata'
      required:
        - geneExpressions
        - page
  parameters:
    modelOverviewSearchQuery:
      name: modelOverviewSearchQuery
      description: The search query used to find and filter model overviews.
      in: query
      style: deepObject
      explode: true
      schema:
        $ref: '#/components/schemas/ModelOverviewSearchQuery'
    diseaseCorrelationSearchQuery:
      name: diseaseCorrelationSearchQuery
      description: The search query used to find and filter disease correlations.
      in: query
      style: deepObject
      explode: true
      schema:
        $ref: '#/components/schemas/DiseaseCorrelationSearchQuery'
    geneExpressionSearchQuery:
      name: geneExpressionSearchQuery
      description: The search query used to find and filter gene expressions.
      in: query
      style: deepObject
      explode: true
      schema:
        $ref: '#/components/schemas/GeneExpressionSearchQuery'
  responses:
    BadRequest:
      description: Invalid request
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/BasicError'
    NotFound:
      description: The specified resource was not found
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/BasicError'
    InternalServerError:
      description: The request cannot be fulfilled due to an unexpected server error
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/BasicError'
