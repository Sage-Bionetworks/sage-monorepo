# No `discriminator:` block: it makes openapi-generator emit Jackson polymorphism
# annotations that overwrite each variant's `type` with the schema name on the wire.
# The server stamps `type` ("mouse"/"marmoset") explicitly instead.
description: Model details for a given organism
oneOf:
  - $ref: MouseModel.yaml
  - $ref: MarmosetModel.yaml
