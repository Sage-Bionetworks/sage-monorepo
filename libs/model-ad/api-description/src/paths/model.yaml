get:
  tags:
    - Model
  summary: Get details for a specific model
  description: Retrieve detailed information for a specific model by its name and organism type.
  operationId: getModelByName
  parameters:
    - name: organism
      in: path
      description: The type of organism (e.g., mouse, marmoset)
      required: true
      schema:
        $ref: ../components/schemas/Organism.yaml
    - name: name
      in: path
      description: Name of the model to retrieve
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Model.yaml
      description: Successfully retrieved model details
    '400':
      $ref: ../components/responses/BadRequest.yaml
    '404':
      $ref: ../components/responses/NotFound.yaml
    '500':
      $ref: ../components/responses/InternalServerError.yaml
