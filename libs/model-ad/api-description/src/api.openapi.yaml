openapi: 3.0.3
info:
  version: 1.0.0
  title: Model-AD API
  license:
    name: Apache 2.0
    url: https://github.com/Sage-Bionetworks/sage-monorepo/blob/main/LICENSE.txt
  contact:
    name: Support
    url: https://github.com/Sage-Bionetworks/sage-monorepo
  x-logo:
    url: https://dev.modeladexplorer.org/opengraph/opengraph.png
    altText: Model-AD Explorer
servers:
  - url: http://localhost/v1
tags:
  - name: Data Version
    description: Operations about the data version.
  - name: Model Legacy
    description: Operations about models.
    # x-sage-internal: true # Now defined in the new API service; making this node internal does not
    # prevent merging conflicts because it just centralize the tag description and does not prevent
    # its creation if at least one endpoint references it, so commenting it out for now.
  - name: Comparison Tool Config
    description: Operations about Comparison Tool UI configuration.
paths:
  /data-version:
    $ref: paths/data-version.yaml
  /models/search:
    $ref: paths/model-search.yaml
  /models/{name}:
    $ref: paths/model.yaml
    x-sage-internal: true # Keep the endpoint internal as long as its implemented by the legacy API service
  /comparison-tool-config:
    $ref: paths/comparison-tool-config.yaml
