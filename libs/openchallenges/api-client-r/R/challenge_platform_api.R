#' OpenChallenges REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 1.0.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ChallengePlatform operations
#' @description ChallengePlatformApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetChallengePlatform } \emph{ Get a challenge platform }
#' Returns the challenge platform specified
#'
#' \itemize{
#' \item \emph{ @param } challenge_platform_name character
#' \item \emph{ @returnType } \link{ChallengePlatform} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ChallengePlatform
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ListChallengePlatforms } \emph{ List challenge platforms }
#' List challenge platforms
#'
#' \itemize{
#' \item \emph{ @param } challenge_platform_search_query \link{ChallengePlatformSearchQuery}
#' \item \emph{ @returnType } \link{ChallengePlatformsPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ChallengePlatformsPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetChallengePlatform  ####################
#'
#' library(openapi)
#' var_challenge_platform_name <- "challenge_platform_name_example" # character | The unique identifier of the challenge platform.
#'
#' #Get a challenge platform
#' api_instance <- ChallengePlatformApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetChallengePlatform(var_challenge_platform_namedata_file = "result.txt")
#' result <- api_instance$GetChallengePlatform(var_challenge_platform_name)
#' dput(result)
#'
#'
#' ####################  ListChallengePlatforms  ####################
#'
#' library(openapi)
#' var_challenge_platform_search_query <- ChallengePlatformSearchQuery$new(123, 123, ChallengePlatformSort$new(), ChallengePlatformDirection$new(), "searchTerms_example") # ChallengePlatformSearchQuery | The search query used to find challenge platforms. (Optional)
#'
#' #List challenge platforms
#' api_instance <- ChallengePlatformApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListChallengePlatforms(challenge_platform_search_query = var_challenge_platform_search_querydata_file = "result.txt")
#' result <- api_instance$ListChallengePlatforms(challenge_platform_search_query = var_challenge_platform_search_query)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ChallengePlatformApi <- R6::R6Class(
  "ChallengePlatformApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ChallengePlatformApi.
    #'
    #' @description
    #' Initialize a new ChallengePlatformApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Get a challenge platform
    #'
    #' @description
    #' Get a challenge platform
    #'
    #' @param challenge_platform_name The unique identifier of the challenge platform.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ChallengePlatform
    #' @export
    GetChallengePlatform = function(challenge_platform_name, data_file = NULL, ...) {
      local_var_response <- self$GetChallengePlatformWithHttpInfo(challenge_platform_name, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get a challenge platform
    #'
    #' @description
    #' Get a challenge platform
    #'
    #' @param challenge_platform_name The unique identifier of the challenge platform.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ChallengePlatform) with additional information such as HTTP status code, headers
    #' @export
    GetChallengePlatformWithHttpInfo = function(challenge_platform_name, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`challenge_platform_name`)) {
        stop("Missing required parameter `challenge_platform_name`.")
      }

      if (nchar(`challenge_platform_name`) > 30) {
        stop("Invalid length for `challenge_platform_name` when calling ChallengePlatformApi$GetChallengePlatform, must be smaller than or equal to 30.")
      }
      if (nchar(`challenge_platform_name`) < 3) {
        stop("Invalid length for `challenge_platform_name` when calling ChallengePlatformApi$GetChallengePlatform, must be bigger than or equal to 3.")
      }

      local_var_url_path <- "/challengePlatforms/{challengePlatformName}"
      if (!missing(`challenge_platform_name`)) {
        local_var_url_path <- gsub("\\{challengePlatformName\\}", URLencode(as.character(`challenge_platform_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ChallengePlatform", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List challenge platforms
    #'
    #' @description
    #' List challenge platforms
    #'
    #' @param challenge_platform_search_query (optional) The search query used to find challenge platforms.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ChallengePlatformsPage
    #' @export
    ListChallengePlatforms = function(challenge_platform_search_query = NULL, data_file = NULL, ...) {
      local_var_response <- self$ListChallengePlatformsWithHttpInfo(challenge_platform_search_query, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List challenge platforms
    #'
    #' @description
    #' List challenge platforms
    #'
    #' @param challenge_platform_search_query (optional) The search query used to find challenge platforms.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ChallengePlatformsPage) with additional information such as HTTP status code, headers
    #' @export
    ListChallengePlatformsWithHttpInfo = function(challenge_platform_search_query = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["challengePlatformSearchQuery"]] <- `challenge_platform_search_query`

      local_var_url_path <- "/challengePlatforms"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ChallengePlatformsPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
