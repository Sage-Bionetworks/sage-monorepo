#' OpenChallenges REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 1.0.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ChallengeContribution operations
#' @description ChallengeContributionApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ ListChallengeContributions } \emph{ List challenge contributions }
#' List challenge contributions
#'
#' \itemize{
#' \item \emph{ @param } challenge_id integer
#' \item \emph{ @returnType } \link{ChallengeContributionsPage} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : ChallengeContributionsPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid request
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | The request cannot be fulfilled due to an unexpected server error
#'
#' \item return type : BasicError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  ListChallengeContributions  ####################
#'
#' library(openapi)
#' var_challenge_id <- 56 # integer | The unique identifier of the challenge.
#'
#' #List challenge contributions
#' api_instance <- ChallengeContributionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListChallengeContributions(var_challenge_iddata_file = "result.txt")
#' result <- api_instance$ListChallengeContributions(var_challenge_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ChallengeContributionApi <- R6::R6Class(
  "ChallengeContributionApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ChallengeContributionApi.
    #'
    #' @description
    #' Initialize a new ChallengeContributionApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' List challenge contributions
    #'
    #' @description
    #' List challenge contributions
    #'
    #' @param challenge_id The unique identifier of the challenge.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ChallengeContributionsPage
    #' @export
    ListChallengeContributions = function(challenge_id, data_file = NULL, ...) {
      local_var_response <- self$ListChallengeContributionsWithHttpInfo(challenge_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List challenge contributions
    #'
    #' @description
    #' List challenge contributions
    #'
    #' @param challenge_id The unique identifier of the challenge.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ChallengeContributionsPage) with additional information such as HTTP status code, headers
    #' @export
    ListChallengeContributionsWithHttpInfo = function(challenge_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`challenge_id`)) {
        stop("Missing required parameter `challenge_id`.")
      }


      local_var_url_path <- "/challenges/{challengeId}/contributions"
      if (!missing(`challenge_id`)) {
        local_var_url_path <- gsub("\\{challengeId\\}", URLencode(as.character(`challenge_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ChallengeContributionsPage", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
