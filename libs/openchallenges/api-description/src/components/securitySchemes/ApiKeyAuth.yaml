type: apiKey
in: header
name: X-API-Key
description: API key for programmatic access. API keys inherit the scopes and permissions of the user who created them. Create and manage via /auth/api-keys endpoints.
