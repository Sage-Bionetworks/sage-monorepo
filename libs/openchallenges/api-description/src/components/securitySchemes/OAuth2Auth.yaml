type: oauth2
description: OAuth2 Authorization Code Flow for user authentication via Google/Synapse
flows:
  authorizationCode:
    authorizationUrl: /oauth2/authorize
    tokenUrl: /oauth2/token
    refreshUrl: /oauth2/token
    scopes:
      # OpenID Connect scopes
      openid: OpenID Connect authentication
      profile: Access to user's basic profile information
      email: Access to user's email address

      # OpenChallenges application-specific scopes
      'read:org': Read access to organization resources
      'write:org': Write access to organization resources
      'delete:org': Delete access to organization resources
