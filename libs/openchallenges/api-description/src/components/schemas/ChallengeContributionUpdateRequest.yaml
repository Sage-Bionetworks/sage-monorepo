type: object
description: A challenge contribution update request.
properties:
  organizationId:
    $ref: OrganizationId.yaml
  role:
    $ref: ChallengeContributionRole.yaml
required:
  - organizationId
  - role
