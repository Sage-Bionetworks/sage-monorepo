name: platforms
description: An array of challenge platform ids used to filter the results.
in: query
required: false
schema:
  type: array
  items:
    $ref: ../../../schemas/ChallengePlatformName.yaml
