type: object
description: A page of org memberships
allOf:
  - $ref: ResponsePageMetadata.yaml
  - type: object
    properties:
      orgMemberships:
        description: An array of org memberships
        type: array
        items:
          $ref: OrgMembership.yaml
    required:
      - orgMemberships
