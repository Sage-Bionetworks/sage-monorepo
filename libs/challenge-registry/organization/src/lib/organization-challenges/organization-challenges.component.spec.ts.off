import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OrganizationChallengesComponent } from './organization-challenges.component';

describe('OrganizationChallengesComponent', () => {
  let component: OrganizationChallengesComponent;
  let fixture: ComponentFixture<OrganizationChallengesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [OrganizationChallengesComponent],
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(OrganizationChallengesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
