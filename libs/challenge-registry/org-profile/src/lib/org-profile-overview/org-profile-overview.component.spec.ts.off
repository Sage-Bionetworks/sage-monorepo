import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OrgProfileOverviewComponent } from './org-profile-overview.component';

describe('OrgProfileOverviewComponent', () => {
  let component: OrgProfileOverviewComponent;
  let fixture: ComponentFixture<OrgProfileOverviewComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [OrgProfileOverviewComponent],
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(OrgProfileOverviewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
