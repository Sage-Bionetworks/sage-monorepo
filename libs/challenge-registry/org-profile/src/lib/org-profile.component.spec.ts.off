import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OrgProfilenComponent } from './org-profile.component';

describe('OrgProfilenComponent', () => {
  let component: OrgProfilenComponent;
  let fixture: ComponentFixture<OrgProfilenComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [OrgProfilenComponent],
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(OrgProfilenComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
