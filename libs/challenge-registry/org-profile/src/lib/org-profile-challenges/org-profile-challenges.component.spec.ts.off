import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OrgProfileChallengesComponent } from './org-profile-challenges.component';

describe('OrgProfileChallengesComponent', () => {
  let component: OrgProfileChallengesComponent;
  let fixture: ComponentFixture<OrgProfileChallengesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [OrgProfileChallengesComponent],
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(OrgProfileChallengesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
