import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ChallengeSponsorsComponent } from './challenge-sponsors.component';

describe('ChallengeSponsorsComponent', () => {
  let component: ChallengeSponsorsComponent;
  let fixture: ComponentFixture<ChallengeSponsorsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ChallengeSponsorsComponent],
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ChallengeSponsorsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
