query CohortSelector($dataSet: [String!]!, $related: [String!]!, $feature: [String!], $featureClass: [String]) {
  tags(dataSet: $dataSet, related: $related, feature: $feature, featureClass:  $featureClass){
    characteristics
    display
    color
    name
    sampleCount
    sampleIds
  }
}
