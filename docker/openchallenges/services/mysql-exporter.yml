version: '3.8'

services:
  openchallenges-mysql-exporter:
    image: sagebionetworks/openchallenges-mysql-exporter:latest
    container_name: openchallenges-mysql-exporter
    restart: always
    env_file:
      - ../../../apps/openchallenges/mysql-exporter/.env
    networks:
      - openchallenges
    ports:
      - '9104:9104'
    # deploy:
    #   resources:
    #     limits:
    #       memory: 200M
