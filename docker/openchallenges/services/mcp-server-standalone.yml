services:
  openchallenges-mcp-server:
    image: ghcr.io/sage-bionetworks/openchallenges-mcp-server:latest
    container_name: openchallenges-mcp-server
    restart: always
    ports:
      - '8887:8887'
    environment:
      API_BASE_PATH: https://openchallenges.io/api/v1
    depends_on:
    deploy:
      resources:
        limits:
          memory: 1G
