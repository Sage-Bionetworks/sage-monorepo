services:
  observability-tempo:
    image: ghcr.io/sage-bionetworks/observability-tempo:${OBSERVABILITY_VERSION:-local}
    command: ['-config.file=/etc/tempo.yaml']
    container_name: observability-tempo
    restart: always
    volumes:
      - ../../../apps/observability/tempo/tempo.yaml:/etc/tempo.yaml:ro
    networks:
      - observability
    ports:
      - '8504:8504' # Tempo HTTP (default: 3200)
      - '8505:8505' # OTLP gRPC (default: 4317)
    # deploy:
    #   resources:
    #     limits:
    #       memory: 200M
