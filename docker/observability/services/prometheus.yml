services:
  observability-prometheus:
    image: ghcr.io/sage-bionetworks/observability-prometheus:${AMP_ALS_VERSION:-local}
    container_name: observability-prometheus
    restart: always
    volumes:
      - ../../../apps/observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    networks:
      - observability
    ports:
      - '8407:9090'
    deploy:
      resources:
        limits:
          memory: 200M
