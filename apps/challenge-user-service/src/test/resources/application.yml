spring:
    #admin:
    #  enabled: true
    #  jmx-name: org.springframework.boot:type=Admin,name=SpringApplicationTest
  datasource:
    url: jdbc:h2:mem:challenge
    driver-class-name: org.h2.Driver
  #jpa:
    #hibernate:
    #  ddl-auto: none
    #database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

server:
  port: 8083

eureka:
  client:
    enabled: false

example:
  firstProperty: "test"
