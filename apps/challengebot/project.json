{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/challengebot/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/challengebot",
        "main": "apps/challengebot/src/main.ts",
        "tsConfig": "apps/challengebot/tsconfig.app.json",
        "assets": ["apps/challengebot/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/challengebot/src/environments/environment.ts",
              "with": "apps/challengebot/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "challengebot:build"
      },
      "configurations": {
        "production": {
          "buildTarget": "challengebot:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/challengebot/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/challengebot"],
      "options": {
        "jestConfig": "apps/challengebot/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
