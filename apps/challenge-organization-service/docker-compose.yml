version: "3.8"

services:
  challenge-organization-service:
    image: sagebionetworks/challenge-organization-service:latest
    container_name: challenge-organization-service
    restart: always
    env_file:
      - .env
    networks:
      - challenge
    ports:
      - "8083:8083"
    command: start-dev

networks:
  challenge:
    name: challenge