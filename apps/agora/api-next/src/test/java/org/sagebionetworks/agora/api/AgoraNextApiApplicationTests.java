package org.sagebionetworks.agora.api;

import org.junit.jupiter.api.Test;

class AgoraNextApiApplicationTests {

  @Test
  void applicationCanBeInstantiated() {
    // Basic test to verify the application class structure
    AgoraNextApiApplication app = new AgoraNextApiApplication();
    assert app != null;
  }
}
