#!/usr/bin/env bash

# NOTE: MongoDB is not running when the docker image is being built. Is there a way to process the
# dataset files so that the data are loaded into the MongoDB instance when it start?
echo "Exporting files to MongoDB format"