version: "3.10"

services:
  schematic-api:
    image: ghcr.io/sage-bionetworks/schematic-api:local
    # for building docker image locally if needed
    # build: 
    #   dockerfile: Dockerfile
    # image: sagebionetworks/schematic-aws-api:latest
    container_name: schematic-api
    restart: always
    env_file:
      - .env
    # volumes:
    #   - openchallenges-mariadb:/data/db
    #   - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    networks:
      - schematic
    ports: 
      - "${SERVER_PORT}:80"
      - "443:443"

# volumes:
#   openchallenges-mariadb:
#     name: openchallenges-mariadb

networks:
  schematic:
    name: schematic