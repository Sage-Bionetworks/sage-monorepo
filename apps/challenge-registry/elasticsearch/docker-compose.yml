version: "3.8"

services:
  challenge-registry-elasticsearch:
    image: sagebionetworks/challenge-registry-elasticsearch:latest
    container_name: challenge-registry-elasticsearch
    restart: always
    env_file:
      - .env
    # volumes:
    #   - challenge-registry-elasticsearch:/data/db
    #   - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    networks:
      - challenge-registry
    ports:
      - "9200:9200"

# volumes:
#   challenge-registry-elasticsearch:
#     name: challenge-registry-elasticsearch

networks:
  challenge-registry:
    name: challenge-registry