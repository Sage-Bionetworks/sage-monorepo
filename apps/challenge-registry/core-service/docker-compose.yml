version: "3.8"

services:
  challenge-registry-core-service:
    image: sagebionetworks/challenge-registry-core-service:latest
    container_name: challenge-registry-core-service
    restart: always
    env_file:
      - .env
    networks:
      - challenge-registry
    ports:
      - "8092:8092"
    command: start-dev

networks:
  challenge-registry:
    name: challenge-registry