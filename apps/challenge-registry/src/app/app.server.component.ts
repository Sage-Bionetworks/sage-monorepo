import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'challenge-registry-app-server',
  template: '',
  styleUrls: ['app.server.component.scss'],
  encapsulation: ViewEncapsulation.None,
})
export class AppServerComponent {}
