version: "3.8"

services:
  challenge-registry-organization-service:
    image: sagebionetworks/challenge-registry-organization-service:latest
    container_name: challenge-registry-organization-service
    restart: always
    env_file:
      - .env
    networks:
      - challenge-registry
    ports:
      - "8084:8084"
    command: start-dev

networks:
  challenge-registry:
    name: challenge-registry