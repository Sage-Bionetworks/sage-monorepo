version: "3.8"

services:
  challenge-registry-config-service:
    image: sagebionetworks/challenge-registry-config-service:latest
    container_name: challenge-registry-config-service
    restart: always
    # env_file:
    #   - .env
    networks:
      - challenge
    ports:
      - "8082:8082"
    command: start-dev

networks:
  challenge-registry:
    name: challenge-registry