sage-monorepo-mcp-server:
  welcome-message: 'Welcome to the Sage Monorepo MCP Server!'

server:
  port: 8887
spring:
  application:
    name: sage-monorepo-mcp-server
  main:
    banner-mode: off
  ai:
    mcp:
      server:
        name: sage-monorepo-mcp-server
        version: 1.0.0
        sse-endpoint: /mcp-server/sse
        sse-message-endpoint: /mcp-server/message
