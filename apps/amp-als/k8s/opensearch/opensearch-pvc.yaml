apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: amp-als-opensearch-pvc
  namespace: amp-als
  labels:
    app: amp-als-opensearch
    component: search
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
