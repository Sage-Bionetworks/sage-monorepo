apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: amp-als-postgres-pvc
  namespace: amp-als
  labels:
    app: amp-als-postgres
    component: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # Uncomment if you have a specific storage class
  # storageClassName: standard
