apiVersion: v1
kind: Service
metadata:
  name: amp-als-postgres
  namespace: amp-als
  labels:
    app: amp-als-postgres
    component: database
spec:
  type: ClusterIP
  ports:
    - port: 8401
      targetPort: 8401
      protocol: TCP
      name: postgres
  selector:
    app: amp-als-postgres
