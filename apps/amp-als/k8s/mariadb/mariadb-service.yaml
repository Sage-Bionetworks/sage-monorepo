apiVersion: v1
kind: Service
metadata:
  name: amp-als-mariadb
  namespace: amp-als
  labels:
    app: amp-als-mariadb
    component: database
spec:
  type: ClusterIP
  ports:
    - port: 8401
      targetPort: 8401
      protocol: TCP
      name: mysql
  selector:
    app: amp-als-mariadb
