AWS_PROFILE=bixarena-Administrator

# Terraform backend (prefer env vars over committing values to config.yaml)
TERRAFORM_BACKEND_BUCKET=sage-bixarena-bootstrap-terraform-backend-use1-prod
TERRAFORM_BACKEND_REGION=us-east-1
TERRAFORM_BACKEND_DDB_TABLE=sage-bixarena-bootstrap-terraform-backend-lock-use1-prod
# Optional: role to assume for remote state access (omit if using current caller)
# TERRAFORM_BACKEND_ROLE_ARN=arn:aws:iam::123456789012:role/AdministratorAccess

# GitHub OIDC (existing provider ARN to reuse)
# GITHUB_OIDC_PROVIDER_ARN=arn:aws:iam::123456789012:oidc-provider/token.actions.githubusercontent.com