# Staging environment configuration
# This file is loaded when running commands with :stage configuration
# Example: nx synth bixarena-infra-cdk:stage

# Required: Environment name
ENV=stage

# Optional: AWS Profile for staging account
# AWS_PROFILE=bixarena-stage

# Optional: Specific AWS region for staging (if different from default)
# AWS_REGION=us-east-1

# Optional: VPC CIDR block (default: 10.0.0.0/16)
VPC_CIDR=10.254.121.0/24

# Optional: Maximum number of Availability Zones (default: 2)
# Stage environment uses one NAT Gateway per AZ for high availability
# Increasing this will increase costs proportionally (~$32.50 per NAT/month)
# MAX_AZS=2

# Optional: Application version/Docker image tag (default: latest)
# APP_VERSION=1.3.8

# Optional: Fully qualified domain name for the application
# If not provided, the ALB DNS name will be used automatically
# Use this when you have a custom domain configured with Route53
FQDN=stage.bixarena.ai

# Optional: ACM Certificate ARN for HTTPS
# If not provided, ALB will run in HTTP-only mode
# For staging, you should have an ACM certificate configured
CERTIFICATE_ARN=arn:aws:acm:us-east-1:045984464920:certificate/2d81bfca-89ea-42cb-8e78-ea9c3d1f6919
