# Base environment configuration for BixArena CDK
# This file is loaded for all environments (dev, stage, prod)
# Environment-specific files (.env.dev, .env.stage, .env.prod) override these values

# Optional: AWS Profile for authentication
# Specify the AWS profile name configured in ~/.aws/credentials
# If not provided, uses the default AWS profile or environment credentials
# AWS_PROFILE=bixarena