query DataSets($dataSet: [String!], $sample: [String!], $type: [String!]) {
  dataSets(dataSet: $dataSet, sample: $sample, type: $type) {
    display
    name
    samples {
      name
    }
    type
  }
}

# Variables
# {"sample": ["TCGA-21-5787"]}