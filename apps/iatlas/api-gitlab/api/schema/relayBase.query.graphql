interface BaseResult {
  pageInfo: PageInfo
  totalCount: Int
  page: Int
  items: [BaseNode]
}

interface BaseEdge {
  cursor: String
  node: BaseNode
}

interface BaseNode {
  id: ID
}

type PageInfo {
  hasNextPage: Boolean
  hasPreviousPage: Boolean
  startCursor: String
  endCursor: String
}
