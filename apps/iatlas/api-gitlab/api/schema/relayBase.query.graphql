interface BaseResult {
  pageInfo: PageInfo
  totalCount: Int
  edges: [BaseEdge]
}

interface BaseEdge {
  cursor: String
  node: BaseNode
}

interface BaseNode {
  id: ID
}

type PageInfo {
  hasNextPage: Boolean
  hasPreviousPage: Boolean
  startCursor: String
  endCursor: String
}
