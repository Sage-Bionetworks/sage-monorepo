version: "3.8"

services:
  openchallenges-opensearch:
    image: sagebionetworks/openchallenges-opensearch:latest
    container_name: openchallenges-opensearch
    restart: always
    env_file:
      - .env
    # volumes:
    #   - openchallenges-opensearch:/data/db
    #   - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    networks:
      - openchallenges
    ports:
      - "9200:9200"  # REST API
      - "9600:9600"  # Performance Analyzer

# volumes:
#   openchallenges-opensearch:
#     name: openchallenges-opensearch

networks:
  openchallenges:
    name: openchallenges