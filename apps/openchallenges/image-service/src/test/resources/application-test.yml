spring:
  application:
    name: openchallenges-image-service-test

server:
  port: 8086

app:
  welcome-message: 'Welcome to the image service test.'
  thumbor:
    host: http://localhost:8000/img/
    security-key: changeme
    use-placeholder-images: false
    placeholder-url-template: https://images.placeholders.dev/{width}x{height}

logging:
  level:
    org.sagebionetworks.openchallenges: DEBUG
