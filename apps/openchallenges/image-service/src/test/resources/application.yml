app:
  welcome-message: 'Welcome to the image service (test)'

spring:
  application:
    name: openchallenges-image-service
  profiles:
    active: test
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
