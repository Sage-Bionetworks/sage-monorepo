openchallenges-image-service:
  welcome-message: 'Welcome to the image service.'
  thumbor-host: http://localhost:8082/img/
  thumbor-security-key: changeme

spring:
  application:
    name: openchallenges-image-service
  profiles:
    active: test
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
