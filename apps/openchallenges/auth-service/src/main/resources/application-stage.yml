# Staging profile configuration
spring:
  jpa:
    show-sql: false
  datasource:
    url: jdbc:postgresql://openchallenges-postgres-stage:8091/auth_service

openchallenges:
  auth:
    api-key:
      prefix: oc_stage_
      length: 40

logging:
  level:
    org.sagebionetworks.openchallenges: INFO
