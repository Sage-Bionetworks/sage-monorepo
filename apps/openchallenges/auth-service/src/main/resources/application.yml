server:
  port: 8406

spring:
  application:
    name: amp-als-user-service
  jackson:
    date-format: org.sagebionetworks.amp.als.user.service.RFC3339DateFormat
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
