version: "3.8"

services:
  openchallenges-elasticsearch:
    image: sagebionetworks/openchallenges-elasticsearch:latest
    container_name: openchallenges-elasticsearch
    restart: always
    env_file:
      - .env
    # volumes:
    #   - openchallenges-elasticsearch:/data/db
    #   - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    networks:
      - openchallenges
    ports:
      - "9200:9200"
    deploy:
      resources:
        limits:
          memory: 2G

# volumes:
#   openchallenges-elasticsearch:
#     name: openchallenges-elasticsearch

networks:
  openchallenges:
    name: openchallenges