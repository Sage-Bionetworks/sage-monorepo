version: "3.8"

services:
  openchallenges-opensearch-dashboards:
    image: sagebionetworks/openchallenges-opensearch-dashboards:latest
    container_name: openchallenges-opensearch-dashboards
    restart: always
    env_file:
      - .env
    # volumes:
    #   - openchallenges-opensearch-dashboards:/data/db
    #   - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    networks:
      - openchallenges
    ports:
     - "5601:5601"

# volumes:
#   openchallenges-opensearch-dashboards:
#     name: openchallenges-opensearch-dashboards

networks:
  openchallenges:
    name: openchallenges