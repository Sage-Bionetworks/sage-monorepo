version: "3.8"

services:
  openchallenges-config-service:
    image: sagebionetworks/openchallenges-config-service:latest
    container_name: openchallenges-config-service
    restart: always
    # env_file:
    #   - .env
    networks:
      - challenge
    ports:
      - "8082:8082"
    command: start-dev

networks:
  openchallenges:
    name: openchallenges