# Production environment configuration
# This file is loaded when running commands with :prod configuration
# Example: nx synth openchallenges-infra-cdk:prod

# Required: Environment name
ENV=prod

# Optional: AWS Profile for production account
# AWS_PROFILE=openchallenges-prod

# Optional: Specific AWS region for production (if different from default)
# AWS_REGION=us-east-1
