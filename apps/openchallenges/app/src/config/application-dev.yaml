# Development-specific configuration
# Applied when NODE_ENV=development or ENVIRONMENT=dev
app:
  version: local
  telemetry:
    enabled: false

data:
  updatedOn: '2025-10-19'

google:
  tagManager:
    id: 'GTM-NBR5XD8C'

api:
  docs:
    url: http://localhost:8000/api-docs
  csr:
    url: http://localhost:8000/api/v1
  ssr:
    url: http://openchallenges-apex:8000/api/v1
