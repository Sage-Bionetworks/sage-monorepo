# Development-specific configuration
# Applied when ENVIRONMENT=dev or NODE_ENV=development

app:
  version: dev
  telemetry:
    enabled: false

analytics:
  googleTagManager:
    enabled: false

api:
  docs:
    url: http://localhost:8000/api-docs
  csr:
    url: http://localhost:8000/api/v1
  ssr:
    url: http://openchallenges-apex:8000/api/v1
