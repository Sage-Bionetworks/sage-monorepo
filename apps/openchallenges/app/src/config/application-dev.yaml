# Development-specific configuration
# Applied when ENVIRONMENT=dev or NODE_ENV=development

app:
  version: dev

telemetry:
  enabled: false

analytics:
  googleTagManager:
    enabled: false

api:
  baseUrls:
    csr: http://localhost:8000/api/v1
    ssr: http://openchallenges-apex:8000/api/v1
  docsUrl: http://localhost:8000/api-docs
